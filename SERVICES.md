# 연동할 서비스 목록

> 관련 문서: [PRD.md](PRD.md), [PRINCIPLE.md](PRINCIPLE.md)
>
> **v2.0 (2026-08-30)** — 앱은 각 서비스의 API 클라이언트를 갖지 않는다. 이 문서는 이제
> "앱이 무엇을 구현하는가"가 아니라 **"각 서비스를 어디로 열고, 무엇을 Claude Code에게
> 맡기는가"** 를 적는다. v1.0의 Notion DB 스키마 상세는 앱이 쓰지 않게 되어 제거했다
> (필요하면 git 이력의 `f071054` 시점 문서를 본다).

## 두 갈래로 나뉜다

| 방식 | 무엇을 | 어떻게 |
|---|---|---|
| **런처** (PRD §5.2) | 깊은 작업 — 문서 편집, 티켓 상세, 일정 드래그, 대화 | 펭귄 **우클릭** → 카드 → 하위 항목 → **Chrome 창** |
| **Claude Code** (PRD §5.3) | 말로 시키는 게 빠른 일 — 조회, 요약, 기록, 상태 변경 | 앱이 위임 (연동 방식은 PRD Q3 미정) |

인증은 앱이 갖지 않는다. 런처는 **Chrome 세션**에, Claude Code는 **자체 인증**에 의존한다.

## 런처 카드와 하위 항목 (구현된 상태)

카드 넉 장 중 셋이 서비스이고, 넷째(`POMODORO`)는 URL이 아니라 타이머를 편다.
기본값은 `src/lib/launcher.ts`의 `DEFAULT_LAUNCHER`에 있다. 편집 UI는 후속 작업이라
지금은 그 상수가 유일한 원천이다.

| 카드 | 하위 항목 | URL |
|---|---|---|
| `NOTION` | TODO 보드 · 오늘 페이지 | **미정** — 계획표 워크스페이스의 주소가 필요하다 |
| `JIRA` | 내 티켓 · 스프린트 보드 | **미정** — 사이트 주소가 필요하다 |
| `GOOGLE CALENDAR` | 주간 뷰 · 오늘 | `https://calendar.google.com/calendar/r/week` · `.../r/day` |
| `POMODORO` | 타이머 (앱 자체 기능) | — |

URL이 비어 있는 항목은 열기를 시도하지 않고 "URL이 아직 없어요"를 그 자리에 보여준다.
열 수 있는 스킴은 `http`/`https`뿐이고, 검증은 웹뷰와 Rust 양쪽에서 한다.

---

## JIRA (협업 관리)

- **런처**: 내 티켓 보드 URL을 연다.
- **Claude Code에 맡길 것**: 내 티켓 목록·상세 조회, 완료 처리, 댓글 작성, 스프린트 진행 확인.
- 비고: v1.0에서 개인 API 토큰 발급이 가능한 것은 확인됐으나, 앱이 직접 쓰지 않으므로 무의미해졌다.

## Notion (일정·TODO 관리)

- **런처**: 계획표 워크스페이스의 TODO Database URL을 연다.
- **Claude Code에 맡길 것**: 오늘 할 일 조회·추가·체크, 하루 수행도 기록, 날짜 페이지 생성.
- **데이터 모델** (Claude Code가 알아야 할 최소한): 행 하나가 "하루"이고, 할 일은 그 날짜
  페이지 **본문의 체크박스 블록**이다. 카테고리는 본문 헤딩(`공부`/`기타`), 시간은 항목 텍스트의
  접두어 규칙(예: `09:30 ~ 17:30 AWS 교육`). 행 속성은 `이름`(Title)·`날짜`(Date)·`수행도`(Select,
  완료/일부/미완/기타 — **하루 단위** 달성도).
- 비고: v1.0에서 앱이 이 구조를 직접 다뤘고(M2), v2.0에서 제거했다. 위 요약은 Claude Code에게
  맥락으로 넘기기 위해 남긴다.

## Google Calendar (일정 집결지)

- **런처**: 캘린더 주간 뷰 URL을 연다.
- **Claude Code에 맡길 것**: 일정 조회·등록·수정·삭제, "이거 일정으로 넣어줘" 같은 요청.

## Slack (메신저)

- **런처**: 워크스페이스 URL을 연다. 조회·전송 모두 Slack 웹 UI가 더 낫다.
- Claude Code 위임은 v2.0 범위 밖 (필요해지면 그때 검토).

## Webex (협업 관리·공지)

- **런처**: 공지 스페이스 URL을 연다.
- Claude Code 위임은 v2.0 범위 밖. v1.0에서 걸림돌이던 12시간 토큰 만료(R1)는 앱이 직접
  인증하지 않게 되어 사라졌다.
