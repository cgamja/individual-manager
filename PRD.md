# 업무에만 집중하기 위한 개인 총괄 비서 Desktop App

> PRD v2.0 (2026-08-30) — 방향 전환 반영. v1.0 초안은 2026-08-06.
> 관련 문서: [PRINCIPLE.md](PRINCIPLE.md), [SERVICES.md](SERVICES.md)
>
> **v2.0에서 바뀐 것**: 5개 서비스의 양방향 UI를 앱 안에 만드는 방향을 접고,
> **앱은 펭귄 + 런처 + 뽀모도로**만 소유한다. 서비스와의 조회·기록은 **Claude Code**가 하고,
> 깊은 작업은 **Chrome으로 원래 서비스**를 연다. 근거는 [PRINCIPLE.md](PRINCIPLE.md) 개정 이력.

## 1. 개요

업무 중 Jira, Notion, Google Calendar, Slack, Webex를 오가며 확인·기록하는 데 드는 시간과
집중력 손실을 없애기 위한 **개인용 macOS 상주 데스크톱 앱**.

바탕화면의 펭귄이 앱의 얼굴이다. 펭귄을 누르면 데스크톱 앱 아이콘처럼 서비스 목록이 뜨고,
고르면 그 서비스가 Chrome 창으로 열린다. 조회·기록처럼 "말로 시키는 게 빠른" 일은
Claude Code에게 맡긴다. 뽀모도로 타이머는 앱 자체 기능이고, 펭귄이 남은 시간을 몸으로 알린다.

**한 줄 정의**: 바탕화면의 펭귄 하나가 하루 업무의 진입점이 되는 개인 비서.

## 2. 사용자 및 실행 환경

| 항목 | 확정 내용 |
|---|---|
| 사용자 | 본인 1명 (개인 도구, 배포 없음) |
| 실행 환경 | 개인 PC, macOS 전용, 로컬 실행 |
| 형태 | 메뉴바 상주 + **바탕화면 펭귄**. 펭귄 클릭 → 런처 |
| 인증 | 서비스별 인증은 **Chrome 세션**과 **Claude Code**가 갖는다. 앱은 토큰을 보관하지 않는다 |

## 3. 목표

1. **하루의 진입점을 하나로 만든다** — 펭귄을 누르면 필요한 서비스로 바로 간다.
2. **연동 UI를 다시 만들지 않는다** — 조회·기록은 Claude Code에게 말하고, 깊은 작업은
   원래 서비스의 웹 UI에서 한다 (PRINCIPLE 2).
3. **집중을 돕는다** — 뽀모도로 타이머와 펭귄이 남은 시간·상태를 계속 눈에 보이게 한다.
4. 업무 데이터는 원본 서비스에 남는다. 앱은 설정만 갖는다 (PRINCIPLE 3·4).

## 4. 비목표 (Non-Goals)

- 다른 사용자 배포, 멀티 계정, 서버/클라우드 인프라
- **앱 안에 서비스별 조회·조작 UI를 만드는 것** (v1.0의 M2~M6 방향 — v2.0에서 폐기)
- **앱이 각 서비스의 API를 직접 호출하는 것** — Claude Code가 한다
- 앱 자체 로컬 DB에 업무 데이터 저장 (설정 제외)
- Windows/Linux 지원

## 5. 기능 요구사항

### 5.1 바탕화면 펭귄 (앱의 얼굴)

M2.5에서 구현된 그대로를 유지하고, 여기에 런처와 타이머 표현을 얹는다.

- 바탕화면 위를 스스로 걸어다니고, 유휴 동작(두리번·기지개·몸 털기·발 갈아 딛기)과 졸기를
  오간다. 숨쉬기·눈 깜빡임은 항상 돈다. 커서를 올리면 눈동자가 따라온다.
- **화면 전체를 무작위로 다닌다.** 바닥을 걷다가 가끔 공중으로 떠올라 날개를 저어
  무작위 목적지까지 헤엄쳐 가고, 도착하면 내려앉는다. 오르내릴 때는 진행 방향으로 몸을 눕힌다.
- 드래그로 집어 옮길 수 있다. **놓는 세기에 따라 포물선을 그리며 날아가고**, 벽과 천장에서
  감쇠하며 튕긴 뒤 바닥에 착지한다. 살짝 놓으면 제자리에서 떨어진다.
- **왼쪽 클릭은 빠따다.** 클릭하면 **펭귄이** 야구방망이를 곧바로 한 번 후려친다
  (참고: 쇼핑카트히어로의 방망이). 맞는 쪽이 아니라 **휘두르는 쪽**이다. **1클릭 1회**이고
  연타할 수 있다. 한 번 휘두르고 나면 의기양양하게 싸가지 반응을 한다.
- **휘둘러도 날아가지 않는다.** 펭귄을 날려 보내는 것은 **드래그로 던졌을 때뿐이다**
  — 그래야 휘두르기와 던지기가 서로 다른 조작으로 남는다. 던져서 나는 중에 휘두르면
  그 자리에서 휘두르고 마저 떨어진다.
- **싸가지 반응 5종** — 등 홱 돌리기, 고개만 돌려 외면, 날개 휘휘 젓기, 눈 굴리기,
  엉덩이 흔들기. 연달아 같은 반응이 나오지 않고, 자세는 되돌아오지 않고 그대로 버틴다.
- **킹받는 한마디** — 클릭과 무관하게 **7~18초 간격으로 알아서** 말풍선을 띄운다. 문구는
  미리 적어 둔 목록에서 뽑고, 코어는 "언제 무엇을 뽑았는지"만 안다 (문구는 표현이므로
  웹뷰가 갖는다). 때릴 때마다 떠들면 시끄러워서 클릭과 분리했다.
- **오른쪽 클릭**하면 타이머·설정 창이 **펭귄 바로 옆에서** 열린다. 왼쪽 클릭은 빠따가
  가져갔다. 화면 가장자리에서는 반대쪽으로 접고, 넘치면 화면 안으로 밀어 넣는다.
  트레이 아이콘으로 열면 예전처럼 메뉴바 밑에서 열린다.
- 펭귄 창은 펭귄보다 크다 — 위쪽은 말풍선, 좌우는 방망이가 휘둘러질 자리다.
  **이 여백도 클릭을 먹는다**: 투명해도 macOS에서는 그 창이 히트 테스트를 가져가고,
  클릭 통과를 쓰지 않기로 했으므로(KTD3) 아래 앱으로 넘어가지 않는다. 그래서 여백은
  꼭 필요한 만큼(좌우 44px, 위 64px)만 둔다. 펭귄의 이동 경계도 창 전체가 화면 안에
  들어오도록 잡아, 벽에 붙어도 방망이가 잘리거나 말풍선이 메뉴바 뒤로 숨지 않는다.
- 설정에서 끄고 켤 수 있으며 선택은 유지된다. 기본은 켜짐.
- 키보드 포커스를 뺏지 않고 Cmd-Tab·Dock에 나타나지 않는다.
- 동작 자체는 규칙 + 시드 난수로 만든다 (여기에는 AI를 쓰지 않는다).

### 5.2 런처 — 펭귄을 누르면 뜨는 서비스 아이콘

- 펭귄을 클릭하면 **데스크톱 앱 아이콘처럼 서비스 아이콘이 펼쳐진다**
  (Notion, Jira, Google Calendar, Slack, Webex).
- 아이콘을 고르면 해당 서비스가 **Chrome 창으로 열린다.** 이미 열려 있으면 그 창을 앞으로.
- 어떤 서비스를 띄울지와 각 URL은 설정에서 바꿀 수 있다.
- 런처는 바깥을 클릭하거나 Esc를 누르면 닫힌다. 펭귄은 런처가 열려 있는 동안 멈춰 선다.

### 5.3 Claude Code 위임 — 말로 시키는 조회·기록

- Notion·Jira·Google Calendar에 대한 조회·기록은 **Claude Code에게 맡긴다.** 앱은 각
  서비스의 API 클라이언트를 갖지 않는다 (PRINCIPLE 1).
- Claude Code는 `--dangerously-skip-permissions`로 인증해 두고, 모델은 작업 성격에 따라
  섞어 쓴다 — 판단이 필요하면 Opus, 일반 작업은 Sonnet, 단순 조회·분류는 Haiku.
- 앱에서 Claude Code를 어떻게 띄우고 대화 결과를 어떻게 보여줄지는 **미정 (§9 Q3)**.

### 5.4 뽀모도로 타이머 (앱 자체 기능)

- 기본 25분 집중 / 5분 휴식. 시간 커스터마이즈 가능. 태스크와 연결하지 않는다.
- 타이머 UI는 앱의 별도 화면으로 뜬다.
- **펭귄이 타이머 상태를 몸으로 알린다** — 집중 중엔 조용히 앉아 있고, 남은 시간이 가까워지면
  반응하며, 세션이 끝나면 눈에 띄게 알린다. 펭귄에게 물어보면 남은 시간을 보여준다.
- 종료 시 macOS 알림 (§5.5).

### 5.5 알림 (opt-in)

- **기본은 조용하다.** 뽀모도로 세션 종료 알림만 v2.0 범위다.
- 서비스 이벤트 알림(Jira 티켓, Slack 메시지 등)은 앱이 폴링을 하지 않으므로 범위 밖이다.

## 6. 제거되는 기능 (v1.0 → v2.0)

M2에서 만든 **앱 내장 Notion TODO 기능 전체**를 제거한다. 대상: Notion 연결 설정(토큰·DB 지정),
할 일 조회/추가/토글/편집, 행 만들기, 카테고리, 수행도 4단계.

제거하는 이유는 품질 문제가 아니라 방향 전환이다 — 같은 일을 Claude Code와 Notion 웹 UI가
더 잘한다. 코드는 git 이력에 남으므로 필요하면 되살릴 수 있다.

## 7. 인증

| 대상 | 방식 |
|---|---|
| Notion / Jira / Google Calendar | **Claude Code**가 갖는다. 앱은 관여하지 않는다 |
| 런처가 여는 웹 UI | **Chrome 세션**이 갖는다. 앱은 URL만 안다 |
| 앱 자체 | 인증 없음 — 보관할 토큰이 없다 |

v1.0에서 확정했던 macOS Keychain 토큰 보관(Q2)은 **더 이상 필요하지 않다.** Notion 기능
제거와 함께 `keyring` 사용도 걷어낸다.

## 8. AI 사용 원칙

- **v2.0은 AI(Claude Code)를 연동의 핵심 엔진으로 쓴다.** v1.0의 "AI를 사용하지 않는다"는
  뒤집혔다 — 근거였던 별도 API 비용이 Claude Code 구독으로 해소되기 때문이다.
- 단, **펭귄의 동작·타이머 같은 앱 자체 로직에는 AI를 쓰지 않는다.** 규칙과 난수로 충분하다.
- `--dangerously-skip-permissions`는 확인 없이 파일 쓰기·명령 실행을 허용한다는 뜻이다.
  개인 기기·본인 전용이라는 전제 위에서만 성립한다 (PRINCIPLE 1의 주의).

## 9. 리스크 & 오픈 퀘스천

| # | 항목 | 내용 | 상태 |
|---|---|---|---|
| Q3 | **Claude Code 연동 방식** | 앱이 `claude` CLI를 자식 프로세스로 띄우는가, 터미널을 여는가, 아니면 별도 창에 대화 UI를 두는가. 출력은 어떻게 보여주는가 | **미정 — 착수 전 결정 필요** |
| Q4 | **런처 UX** | 아이콘이 펭귄 주변에 원형으로 펼쳐지는가, 펭귄 위에 격자로 뜨는가. 크기·개수 한계 | 설계 시 결정 |
| Q5 | Chrome 창 제어 | "이미 열려 있으면 앞으로"를 어떻게 판정하는가. `open -a`로 충분한지, AppleScript가 필요한지 | 구현 시 확인 |
| R6 | 펭귄 클릭의 의미 변경 | 클릭 시 **펭귄 옆에서** 창이 열린다(2026-08-30 구현). M4에서 이 자리에 런처를 얹으면 팝오버 진입점은 트레이만 남는다 | 진행 중 |
| Q1 | 기술 스택 | **Tauri v2 + React/TypeScript(Vite)** — M1·M2·M2.5에서 검증 완료 | 확정 ✅ |
| Q2 | 토큰 보관 | ~~macOS Keychain~~ — v2.0에서 **불필요해짐** (§7) | 폐기 ✅ |

v1.0의 R1(Webex 토큰 수명), R2(Slack 설치 승인), R3(Notion DB 스키마), R4(Rate limit)는
앱이 각 서비스 API를 직접 부르지 않게 되면서 **전부 무효**가 됐다.

## 10. 마일스톤

| 단계 | 내용 | 상태 |
|---|---|---|
| M1 | 메뉴바 앱 골격 + 팝오버 + 뽀모도로 + 알림 | 완료 ✅ |
| M2 | Notion TODO 통합 | 완료했으나 **v2.0에서 제거** |
| M2.5 | 바탕화면 펭귄 (창·동작·클릭·드래그·on/off) | 완료 ✅ |
| **M3** | **Notion 기능 제거 + 정리** — 앱을 펭귄 + 타이머만 남긴 상태로 되돌린다 | 다음 |
| **M4** | **런처** — 펭귄 클릭 → 서비스 아이콘 → Chrome 열기 (§5.2) | |
| **M5** | **뽀모도로 ↔ 펭귄 연동** — 타이머 별도 화면 + 펭귄이 상태를 표현 (§5.4) | |
| **M6** | **Claude Code 위임** — Q3 결정 후 착수 (§5.3) | |

M3을 먼저 하는 이유: 제거가 가장 확실하고, 남길 코드가 줄어야 나머지 설계가 단순해진다.
M6을 마지막에 두는 이유: 연동 방식(Q3)이 아직 미정이고 가장 불확실하다.
