# TODO

> [PRD.md](PRD.md) §10 마일스톤 기준. 각 단계가 끝날 때마다 "혼자 쓰기에 유용한 상태"를 유지한다.
>
> **2026-08-30 방향 전환** — 앱 안에 서비스별 UI를 만드는 방향(v1.0의 M2~M6)을 접었다.
> 앱은 **펭귄 + 런처 + 뽀모도로**만 갖고, 서비스 조회·기록은 **Claude Code**가, 깊은 작업은
> **Chrome으로 연 원래 웹 UI**가 맡는다. 근거는 [PRINCIPLE.md](PRINCIPLE.md) 개정 이력.

## 시작 전 결정
- [x] 기술 스택 선택 (Tauri / Electron / Swift 네이티브) — PRD Q1 → **Tauri v2 + React/TS 확정** (2026-08-06)
- [x] ~~토큰 보관 방식 확정 — macOS Keychain~~ → v2.0에서 **불필요해짐** (앱이 토큰을 갖지 않는다)
- [x] 기존 Notion DB 스키마 확인 — v2.0에서 무효 (앱이 Notion API를 부르지 않는다)
- [ ] **Claude Code 연동 방식 결정 (PRD Q3)** — M6 착수 전 필요. `claude` CLI를 자식 프로세스로
      띄울지, 터미널을 열지, 별도 창에 대화 UI를 둘지. 출력을 어떻게 보여줄지

## 개발 환경
- [x] 개발 워크플로우 정비: `CLAUDE.md`(상시 컨텍스트) + `develop` 스킬(플랜→PR 절차) (2026-08-09)
- [x] `develop` 스킬 2~4단계에 compound-engineering 엔진 사용 의무화 (2026-08-09)

## 완료된 마일스톤

### M1 — 앱 골격 + 뽀모도로
- [x] macOS 메뉴바 상주 앱 골격 (펭귄 아이콘)
- [x] 카드형 팝오버 UI 기본 레이아웃
- [x] 뽀모도로 타이머 (25/5 기본, 시간 커스터마이즈)
- [x] macOS 알림 권한 + 타이머 종료 알림

### M2 — Notion TODO (v2.0에서 제거 대상)
> 전부 구현·머지됐으나 방향 전환으로 걷어낸다. 이력은 git에 남는다.
- [x] Notion Integration 연결·DB 지정, 할 일 조회/추가/토글/편집, 행 만들기,
      카테고리(공부/기타), 수행도 4단계 (2026-08-09 ~ 2026-08-10)

### M2.5 — 바탕화면 펭귄
- [x] 대사 목록 편집 — 우클릭으로 연 창에서 추가·수정·삭제 (2026-08-30)
- [x] 킹받는 한마디(말풍선) + 클릭 빠따(1클릭 1회, 연타 가능) — 왼쪽은 빠따,
      오른쪽은 창 열기로 갈랐다 (2026-08-30)
- [x] 클릭 반응을 싸가지 있게 + 그 자리에서 창 열기 — 공중에서 눌러도 바닥으로
      끌려가지 않고 제자리에서 반응한다 (2026-08-30)
- [x] 화면 전체 무작위 이동 + 던지기 포물선 — 공중을 헤엄쳐 위아래로도 다니고, 놓는
      세기에 따라 포물선을 그린다. 벽·천장에서 감쇠하며 튕긴다 (2026-08-30)
- [x] 펭귄 창 + 이동/유휴 동작 + 클릭·드래그 + 시선 추적 + 설정 on/off — 투명·무프레임 창,
      Tauri 무의존 코어(`pet.rs`), Rust 20Hz 틱이 창 위치를, 웹뷰 CSS가 부위 모션을 맡는다
      (2026-08-30, PR #11)

## M3 — Notion 기능 제거 + 정리 (완료 ✅)
> 남길 코드가 줄어야 뒤 설계가 단순해진다. 제거가 가장 확실한 작업이라 먼저 한다.

- [x] 앱 내장 Notion 기능 제거 — 코드에 남은 흔적 없음 (2026-08-30 확인)
- [x] 딸려 있던 의존성 정리 — `keyring`·`reqwest`·`chrono` 모두 `Cargo.toml`에 없음 (2026-08-30 확인)
- [x] `SERVICES.md`를 v2.0에 맞게 정리 — 런처 카드·하위 항목·URL 표를 실제 구현에 맞춤 (2026-08-30)
- [x] 팝오버에 타이머·설정만 남은 상태로 정리 — M4에서 그 위에 런처를 얹었다 (2026-08-30)

## M4 — 런처 (펭귄 우클릭 → 카드 4장 → Chrome) ← 진행 중
- [x] 카드 4장이 호를 그리며 펼쳐지는 런처 + 카드마다 2단 토글 —
      `NOTION`/`JIRA`/`GOOGLE CALENDAR`/`POMODORO`, Q4를 팬(스택) 레이아웃으로 확정 (2026-08-30)
- [x] 하위 항목 선택 시 Chrome 창으로 열기 — `open -a "Google Chrome"`, 앱 수준 전면화 (2026-08-30)
- [ ] 런처에 띄울 서비스 목록·URL 설정 — 저장소 키(`launcher`)와 타입은 이미 있고 편집 UI만 남았다.
      **Notion 계획표 TODO DB URL과 Jira 보드 URL이 아직 비어 있다**
- [x] 펭귄 우클릭의 기존 동작(팝오버 열기)을 런처로 교체 — 트레이 진입점도 같은 런처를 연다 (R6 해소, 2026-08-30)
- [ ] Chrome **탭 단위** 중복 방지 — 같은 URL 탭이 이미 있으면 그 탭으로. AppleScript가 필요하다 (Q5 잔여)

## M5 — 뽀모도로 ↔ 펭귄 연동
- [ ] 타이머를 앱의 별도 화면으로 분리
- [ ] 펭귄이 타이머 상태를 표현 — 집중 중엔 조용히, 종료 시 눈에 띄게 (PRD §5.4)
- [ ] 펭귄에게 물어보면 남은 시간을 보여주기

## M6 — Claude Code 위임
> **선행 조건: PRD Q3(연동 방식) 결정.** 미정인 채로 착수하지 않는다.

- [ ] Notion·Jira·Google Calendar 조회·기록을 Claude Code에 위임
- [ ] 모델 라우팅 (판단=Opus / 일반=Sonnet / 단순 조회=Haiku)
- [ ] 대화 진입점과 결과 표시

## 후속 (급하지 않음)
- [ ] 멀티 모니터 — 펭귄과 팝오버 모두 현재 단일 모니터를 전제한다
- [ ] 흑백 템플릿 트레이 아이콘 (다크/라이트 메뉴바 자동 적응)
- [ ] 로그인 시 자동 실행 (autostart)
- [ ] 펭귄 이동 동작 확장 — 배 썰매(tobogganing), 점프, 화면 가장자리에 기대기
