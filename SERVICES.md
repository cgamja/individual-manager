# 연동할 서비스 목록

> 5개 서비스 모두 v1 범위, 양방향 통합. 인증 실현 가능성은 전부 검증 완료. 관련 문서: [PRD.md](PRD.md)

### JIRA (협업 관리)
나의 task를 확인하고 TODO로써 수행할 때, 사이트에 접속하지 않고 앱에서 바로 관리한다.
- 범위: 내 티켓 목록·상세 조회, 완료(상태 전환) 처리, 댓글 작성, 스프린트 진행상황 확인
- 인증: Jira Cloud, 개인 API 토큰 (발급 확인 ✅)

### Notion (일정·TODO 관리 — Source of Truth)
내 일정 및 TODO는 모두 Notion의 별도 페이지 Database에서 TODO 템플릿으로 관리하고 있다.
기존 템플릿은 운동·공부·기타가 섞여 있는데 시간이나 세부 사항을 적을 수 없어 불편하다.
TODO는 이 서비스에서 관리하되, **기록은 계속 Notion Database에 남긴다** (앱은 클라이언트, Notion이 유일한 저장소).
- 범위: 할 일 생성·수정·체크·조회, 카테고리·시간·세부 메모 표현, 수행도 4단계(완료/일부/미완/기타) 처리
- 인증: Internal Integration Token (생성 확인 ✅) — macOS Keychain 보관, Rust 전용

#### 실제 DB 구조 (2026-08-09 확인 완료, PRD R3 해소)

**행 하나가 "할 일"이 아니라 "하루"다.** 이 전제가 M2 설계 전체를 규정한다.

| 속성 | 타입 | 실제 사용 양상 |
|---|---|---|
| `이름` | Title | 거의 전부 `[TODO]` (템플릿 이름 그대로). 예외는 `휴일`·`MT`·`vacation` 등 |
| `날짜` | Date | 전 건 date-only (시간 미사용). 날짜 범위 사용 3건 |
| `수행도` | Select | `완료` / `미완` / `일부` / `기타` — **그 날 전체의 달성도** |

- 할 일은 **날짜 페이지 본문의 `to_do` 블록**에 들어간다. DB 행이 아니다.
- 카테고리는 속성이 아니라 **본문 헤딩**이다 — 기본 템플릿이 `### 공부` / `### 기타` 두 개.
  운동은 `기타` 밑에 텍스트로 들어간다.
- 시간은 이미 **할 일 텍스트의 접두어**로 손수 적고 있다 (`09:30 ~ 17:30 AWS 교육`, `19:00 팀 회의`).
  앱은 이 관습을 규칙으로 정식화해 파싱·입력한다.
- `기타`는 휴일·휴가처럼 할 일 자체가 없는 날에 쓴다.
- DB에 `[TODO]` 페이지 템플릿이 있어 새 날짜를 만들 때 헤딩 두 개가 자동으로 깔린다.
- 규모: 2026-05-17 ~ 2026-08-07, 80행 (완료 51 / 일부 23 / 기타 5 / 미완 1). 빈 `수행도` 없음.
- DB는 기간 페이지(`s2026.05.17 ~ 2026.12`) 아래에 있다 → **기간이 바뀌면 DB가 바뀔 수 있으므로
  대상 DB는 설정에서 교체 가능해야 한다.**

**결정**: 기존 구조를 바꾸지 않고 블록 기반으로 붙인다. 기존 80행 마이그레이션 없음, Notion에서
쓰던 캘린더 뷰·수행도 도넛 차트도 그대로 유지된다 (PRINCIPLE 4 — 앱은 클라이언트, Notion이 저장소).

대상 DB ID는 문서·코드에 하드코딩하지 않고 앱 설정에서 입력받는다.

#### 연결 방식 (2026-08-09 구현, M2 첫 항목)

- API 버전은 `2025-09-03`로 상수 고정. 이 버전부터 database는 컨테이너이고 스키마는 **data source**에
  있어, 검증은 2단계다: `GET /v1/databases/:id` → `data_sources[0].id` → `GET /v1/data_sources/:id`의
  `properties`에서 `날짜`(date)·`수행도`(select) 확인. 검증 성공 시 database_id와 **data_source_id**를
  함께 로컬 설정(`settings.json`의 `notion` 키)에 저장한다 — 이후 조회/쓰기는 data_source ID를 쓴다.
- DB 지정은 Notion URL/ID 붙여넣기(뷰 ID `?v=` 오인 방지, 32자 hex → UUID 정규화). 필수 스키마가
  없으면 연결 실패로 처리한다. 404는 "미공유 또는 원본 링크 아님" 힌트로 안내한다.
- 토큰은 Keychain(`keyring` crate)에만 저장하고 Rust 밖으로 내보내지 않는다. dev 빌드는 재빌드마다
  Keychain 접근 프롬프트가 반복되는 것이 정상(ad-hoc 서명) — 최종 UX 검증은 번들 `.app`에서.
- 429는 클라이언트 계층에서 `Retry-After` 준수 + 지수 백오프(3회 상한)로 처리한다.

#### 할 일 블록 조회·쓰기 (2026-08-09 구현, M2 세 번째 항목)

- 하루 행 조회는 **범위 인식**이다: Notion 날짜 필터는 범위 값(start+end) 평가를 문서화하지 않아
  (통설: 시작일만 비교) `equals`가 기간 중간 날짜를 못 잡는다. 그래서
  `POST /v1/data_sources/:id/query`에 `날짜` `on_or_after`(대상일−31일)+`on_or_before`(대상일)
  and 필터 + 내림차순 정렬 + `page_size` 100을 보내고, `start ≤ 대상일 ≤ (end ?? start)` 판정은
  앱이 한다 — `휴일 8/12~8/14` 같은 범위 행이 기간 중간 날짜에도 잡힌다. "오늘"은 Rust가
  `chrono::Local`로 판정한다(웹뷰 시계 불신). 같은 날짜에 행이 여럿이면 제목 `[TODO]` 행을 우선
  선택하고, 선택된 페이지 제목을 카드에 노출한다(`휴일`·`MT` 행 대비).
- 할 일은 페이지 본문 최상위 `to_do` 블록만 다룬다(`GET /v1/blocks/:page_id/children` 페이지네이션).
  추가는 본문 끝 append, 토글은 `checked`만, 텍스트 편집은 `rich_text` 전체 교체(플레인 텍스트,
  2000자 제한 — 서식 있는 항목은 서식이 사라짐).
- **쓰기 후 재조회는 아는 `page_id`의 children 조회로 시작**한다(날짜 재쿼리는 404 폴백) —
  자정 경계·생성 직후 인덱싱 지연 대비. 블록 소실(404)·편집 충돌(409)은 오류가 아니라
  자동 재조회 + 안내 문구로 처리한다. 쓰기가 반영된 뒤 재조회만 실패하면(비멱등 append의
  중복 방지) 오류 대신 스냅샷 없는 안내를 돌려주고 프론트는 기존 목록을 유지한다.
  그 외 실패(네트워크·인증·한도)는 오류로 전달하고, **프론트가** 배너 표시와 함께 목록을
  1회 재조회한다(입력값은 유지).
- 오늘 페이지 생성은 `template` 파라미터(비동기 적용) 대신 `children` 골격 방식: 제목 `[TODO]`,
  `날짜`=오늘, `heading_3` 헤딩 2개(`공부`/`기타`). 제목 속성 키는 하드코딩하지 않고 생성 시점에
  data source 스키마에서 `title` 타입 속성을 찾는다. 생성 시 가장 최근 `[TODO]` 행의 아이콘을
  복사한다(제목 title 필터 + `날짜` 내림차순 1건). 이모지·외부 URL 타입만 복사하고
  file·custom_emoji 타입은 기본 이모지(📝)로 폴백하며, 아이콘 조회 실패는 생성을 막지 않는다.
- **행 만들기**(카드 인라인 폼, M2): 제목이 `[TODO]`면 하루 단위 골격 페이지(공부/기타 헤딩 +
  복사 아이콘), 아니면 특수 행 — `날짜` start(+end 범위 가능), `수행도` select(기본 `기타`),
  이모지 아이콘 지정 가능, 본문 골격 없음. 생성 전 같은 성격의 행(제목 `[TODO]`끼리 / 같은 제목끼리)을
  구간 겹침으로 검사해, 있으면 생성하지 않고 `exists`(page_id)를 돌려주며 프론트가 "기존 행 열기"를
  제공한다. 열기는 page_id 기반(같은 날짜에 `[TODO]`와 특수 행이 공존해도 정확히 그 행을 연다),
  404면 날짜 조회로 폴백.
- **날짜 전환**: 스냅샷에 `is_today`를 실어 내려보내고(오늘 판정은 Rust), 쓰기 커맨드는 스냅샷의
  날짜를 되돌려받아 그 날짜로 재조회한다. 팝오버 재표시 시의 오늘 재조회가 "오늘 복귀"를 겸한다.
- API 버전은 `2025-09-03` 유지. 최신 `2026-03-11`과의 차이는 이 표면에서 append `after`→`position`,
  `archived`→`in_trash` 뿐이다(승격은 별도 작업, TODO.md 후속 항목).

### Google Calendar (일정 집결지)
내 모든 일정(JIRA, Notion, Slack, Webex에서 나온 것 포함)을 Google Calendar에 모은다.
일일이 사이트에 접속해 작성하지 않고, 다른 서비스의 내용을 보다가 "일정으로 등록" 액션으로 추가한다 (판단은 항상 사용자가, 자동 추출 없음).
- 범위: 일정 등록·수정·삭제·조회, 타 서비스 → 캘린더 수동 브릿지
- 인증: 개인 Google 계정, 개인 GCP 프로젝트 + OAuth

### Slack (메신저)
- 범위: 내가 선택한 채널의 메시지 조회, 특정 채널로 메시지 전송
- 비범위: DM 관리, 스레드 전체 기능
- 인증: 개인 Slack App (앱 생성 및 Install 버튼 확인 ✅ — 실제 설치·스코프 승인은 개발 초기에 검증)

### Webex (협업 관리·공지)
공지사항이 많이 올라오는데 내용을 정리하거나 필요한 내용을 캐치하기 어렵다.
- 범위: 공지 스페이스 메시지 모아보기, 키워드 필터/하이라이트(예: "배포", "장애", 내 이름)
- 인증: ⚠️ 개인 액세스 토큰은 12시간 만료 → **OAuth Integration + refresh token 방식 필수**
