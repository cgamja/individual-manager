{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window, every pet window (pet-<id>), the pinball board, and the bowling ball",
  "windows": [
    "main",
    "pet-*",
    "pinball-board-*",
    "bowling-ball"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "store:default"
  ]
}
