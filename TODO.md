# TODO

> [PRD.md](PRD.md) §10 마일스톤 기준. 각 단계가 끝날 때마다 "혼자 쓰기에 유용한 상태"를 유지한다.

## 시작 전 결정
- [x] 기술 스택 선택 (Tauri / Electron / Swift 네이티브) — PRD Q1 → **Tauri v2 + React/TS 확정** (2026-08-06)
- [x] 토큰 보관 방식 확정 — macOS Keychain (`keyring` crate, Rust 전용) — PRD Q2 (2026-08-09)
- [x] 기존 Notion DB 스키마 확인 — 상태 속성 `수행도`(완료/미완/일부/기타) 이미 존재.
      단, **행 하나가 "하루"이고 할 일은 페이지 본문 체크박스**임이 확인돼 M2를 블록 기반으로
      확정 — PRD R3 (2026-08-09)

## 개발 환경
- [x] 개발 워크플로우 정비: `CLAUDE.md`(상시 컨텍스트) + `develop` 스킬(플랜→PR 절차) (2026-08-09)
- [x] `develop` 스킬 2~4단계에 compound-engineering 엔진 사용 의무화 (2026-08-09)

## M1 — 앱 골격 + 뽀모도로
- [x] macOS 메뉴바 상주 앱 골격 (펭귄 아이콘 `penguin-icon.png`)
- [x] 카드형 팝오버 UI 기본 레이아웃
- [x] 뽀모도로 타이머 (25/5 기본, 시간 커스터마이즈)
- [x] macOS 알림 권한 + 타이머 종료 알림

## M1 후속 — 플랜에서 미룬 항목
> M1 플랜의 "Deferred to Follow-Up Work". 급하지 않으나 잊지 않기 위해 옮겨둔다.

- [ ] 멀티 모니터에서 팝오버 위치 보정 (positioner가 단일 모니터를 전제)
- [ ] 흑백 템플릿 트레이 아이콘 (다크/라이트 메뉴바 자동 적응)
- [ ] 로그인 시 자동 실행 (autostart)

## M2 — Notion TODO
> 데이터 모델 확정(2026-08-09): **행 = 하루**, 할 일 = 날짜 페이지 본문의 체크박스 블록.
> 카테고리는 본문 헤딩(`공부`/`기타`), 시간은 항목 텍스트 접두어. 상세는 [SERVICES.md](SERVICES.md).

- [x] Notion Integration 연결 및 대상 Database 지정 — 토큰은 Keychain, DB는 URL/ID 붙여넣기,
      저장 시 자동 검증(`날짜`/`수행도` 스키마 확인) + 수동 연결 테스트 (2026-08-09)
- [ ] (후속) 접근 가능한 DB 목록에서 선택하는 지정 UI — URL 붙여넣기로 불편해질 때 검토
- [x] 날짜 페이지의 할 일(체크박스 블록) 조회/생성/수정 카드 UI — 오늘 행 조회([TODO] 제목 우선),
      to_do 추가/토글/텍스트 편집, 오늘 페이지 골격 생성, 쓰기 후 page_id 기반 재조회 (2026-08-09)
- [x] 오늘 페이지 생성 시 최신 [TODO] 행 아이콘 복사 — 이모지·외부 URL만, file/custom_emoji는 기본 이모지 폴백 (2026-08-10)
- [x] 행 만들기 카드 확장 — 인라인 폼으로 미래 [TODO] 행 생성(골격+아이콘 복사), 중복은 기존 행
      열기(page_id 기반), 생성·열기 후 그 날짜로 카드 전환(오늘로 돌아가기 포함), 하루 조회를 범위
      인식으로 확장. 특수 행(휴일/MT) 생성은 구현 후 사용 피드백으로 제거 (2026-08-10)
- [x] 카테고리(공부/기타) 조회·삽입 — 목록을 헤딩 기준으로 나눠 표시하고, 추가 시 카테고리를 골라
      해당 헤딩 섹션 끝에 `after` 삽입 (표현 확장 항목의 카테고리 몫 선반영, 2026-08-10)
- [ ] (후속) Notion API 버전 `2026-03-11` 승격 — 이 기능 표면에서는 append `after`→`position`,
      `archived`→`in_trash` 두 가지만 바뀜. 별도 마이그레이션 작업으로만 진행
- [ ] 할 일 표현 확장 (시간/소요시간 텍스트 규칙, 세부 메모 — 카테고리 헤딩은 행 만들기 확장에서 선반영됨)
- [x] 수행도 4단계(완료/일부/미완/기타) 처리 — 보고 있는 날짜를 덮는 행의 `수행도`를 카드에서 표시·
      즉시 변경, 값 없으면 "미지정", 범위 행은 적용 구간 표시 (2026-08-10)
- [ ] 동기화 검증 루프 (주기 검증 + 오류 시 전체 재동기화 + 쓰기 재시도 큐)

## M2.5 — 바탕화면 펭귄
> 사용자 요청으로 M2의 남은 항목(동기화 검증 루프)보다 앞당겼다 (2026-08-30).
> 상세는 [PRD.md](PRD.md) §5.8, 설계는
> [플랜](docs/plans/2026-08-30-006-feat-desktop-pet-penguin-plan.md).

- [x] 바탕화면 펭귄 창 + 이동/유휴 동작 + 클릭·드래그 + 설정 on/off — 투명·무프레임 창을
      별도 Vite 엔트리로 띄우고, Tauri 무의존 코어(`pet.rs`)가 동작을, Rust 20Hz 틱이 창
      위치를, 웹뷰 CSS가 부위 모션을 맡는다. 펭귄 그림은 기존 사진을 옮겨 그린 레이어드
      SVG다 (2026-08-30)
- [ ] (후속) 뽀모도로·할 일 연동 무드 — 집중 중엔 조용히, 세션 종료 시 환호, 휴식엔 활발히
- [ ] (후속) 이동 동작 확장 — 배 썰매(tobogganing), 점프, 화면 가장자리에 기대기
- [ ] (후속) 멀티 모니터 — 현재는 펭귄이 있는 모니터 하나만 쓴다.
      M1 후속의 "멀티 모니터 팝오버 위치 보정"과 함께 다룬다

## M3 — Jira
- [ ] Jira Cloud API 토큰 연결
- [ ] 내 티켓 목록·상세 조회
- [ ] 완료(상태 전환) 처리, 댓글 작성
- [ ] 스프린트 진행상황 뷰
- [ ] 알림: 티켓 할당 / 내 티켓에 댓글 (opt-in)

## M4 — Google Calendar
- [ ] 개인 GCP 프로젝트 + OAuth 연결
- [ ] 일정 등록/수정/삭제/조회
- [ ] "일정으로 등록" 브릿지 (Notion·Jira → Calendar 먼저, 이후 Slack·Webex)
- [ ] 알림: 다가오는 일정 N분 전 (opt-in)

## M5 — Slack
- [ ] Slack App 생성·설치·스코프 승인 실검증 (PRD R2)
- [ ] 선택한 채널 메시지 조회
- [ ] 특정 채널 메시지 전송
- [ ] 알림: 지켜보는 채널 새 메시지 (opt-in)

## M6 — Webex
- [ ] OAuth Integration + refresh token 흐름 구현 (12h 토큰 만료 대응, PRD R1)
- [ ] 공지 스페이스 메시지 모아보기
- [ ] 키워드 필터/하이라이트
- [ ] 알림: 새 공지 / 키워드 매칭 (opt-in)
