# 서비스가 지켜야 할 원칙

> 관련 문서: [PRD.md](PRD.md), [MOTIONS.md](MOTIONS.md)
>
> **개정 (2026-08-31, v3.0)**: 업무 기능을 전부 걷어내고 재미 전용으로 전환하면서
> 원칙 1~4를 다시 썼다. 개정 전 원칙(v1.0·v2.0)은 문서 맨 아래 "개정 이력"에 남긴다 —
> **왜 두 번이나 뒤집혔는지 모르면 다음에 같은 논의를 처음부터 다시 한다.**

### 1. 쓸모를 목적으로 삼지 않는다

이 앱은 생산성 도구가 아니다. 할 일을 알려주지 않고, 시간을 재지 않고, 다른 서비스를
열지 않는다. **유일한 성공 기준은 "보고 있으면 웃긴가"다.**

- 새 기능을 넣고 싶을 때 "이게 업무에 도움이 되나"로 정당화하지 않는다. 그 논리가
  v1.0(앱 안에 5개 서비스 UI)과 v2.0(런처 + Claude Code 위임)을 만들었고, 둘 다 접었다.
- 쓸모가 필요해지면 **다른 도구를 쓴다.** 이 앱에 붙이지 않는다.
- 재미의 반대말은 "쓸모없음"이 아니라 **"예측 가능함"**이다. 늘 같은 동작만 반복하면
  귀여운 gif와 다를 게 없다.

### 2. 앱은 하나, 펭귄 마릿수는 사용자가 정한다

- **앱 인스턴스는 반드시 하나다.** 두 개가 뜨면 펭귄이 유령처럼 겹쳐 보이고, 어느 쪽이
  어떤 상태인지 알 수 없고, 설정을 고쳐도 다른 쪽은 모른다. 두 번째 실행은 기존 인스턴스를
  깨우고 **스스로 물러난다.**
- **펭귄 마릿수는 사용자가 고른다** — 우클릭으로 추가하고 삭제한다. 기본은 한 마리다.
  **사고로 늘어나는 것과 골라서 늘리는 것은 전혀 다르다** — 앞은 버그고 뒤는 기능이다.
  이 구분이 이 원칙의 전부다.
- **세계는 화면 하나다.** 펭귄은 자기가 떠 있는 화면 안에서만 논다. **모니터 경계는
  통로가 아니라 벽이다.** 여러 화면을 넘어 다니는 것은 v3.2에서 범위 밖으로 뺐다 —
  아래 개정 이력을 읽지 않으면 같은 논의를 처음부터 다시 하게 된다.
- 펭귄을 다른 화면에서 보고 싶으면 **집어서 옮긴다.** 드래그는 이미 창을 따라가므로
  경계를 넘는 유일한 길이고, 그건 사용자의 손이지 펭귄의 자율 이동이 아니다.

### 3. 동작은 규칙과 시드 난수로 만든다 — AI를 쓰지 않는다

- 펭귄의 이동·모션 선택·대사 추첨에 AI나 네트워크를 쓰지 않는다. 규칙과 난수로 충분하고,
  오프라인에서 돌고, 비용이 없고, **테스트할 수 있다.**
- 난수를 쓰되 **시드를 받는다.** 같은 시드에 같은 결과가 나와야 동작을 단위 테스트로
  묶어둘 수 있다. 코어(`pet.rs`)가 Tauri에 의존하지 않는 이유도 같다.
- 앱 런타임의 이야기다 — **개발에 Claude Code를 쓰는 것과는 무관하다.**

### 4. 상태의 주인을 나눈다 — 코어는 무엇을, 웹뷰는 어떻게

- **Rust 코어**는 "지금 무슨 동작인지, 어디에 있는지"만 안다. 20Hz 틱이 창 위치의
  권위 있는 원천이다.
- **웹뷰**는 "그 동작이 어떻게 보이는지"를 갖는다 — CSS 부위 모션, 대사 문구, 스프라이트.
  같은 사실을 양쪽이 각자 계산하게 두지 않는다.
- 숨겨진 웹뷰의 JS 타이머는 ~5분 뒤 멈춘다. **주기 동작의 진실 원천은 항상 Rust 쪽이다.**

### 5. 방해하지 않는다

바탕화면에 상주하는 앱이 지켜야 할 최소한이다. 재미는 여기서 예외를 얻지 못한다.

- 키보드 포커스를 뺏지 않고, Cmd-Tab·Dock에 나타나지 않는다.
- **기본값은 조용하다.** 소리는 opt-in이고 기본 꺼짐이다 (PRD §5.5). 알림은 보내지 않는다.
- 언제든 설정에서 끌 수 있고, 그 선택은 유지된다.
- 앱은 네트워크를 쓰지 않고, 설정 세 가지 말고는 아무것도 저장하지 않는다.

---

## 개정 이력

### 2026-08-31 (v3.2) — 모니터 경계 넘기를 범위 밖으로

**개정 전 원칙 2**: "세계는 넓다. 연결된 모든 화면이 놀이터다. 펭귄은 모니터 경계를
통로로 보고 넘어 다닌다."
→ **제외했다.** 접은 게 아니라 아예 뺀다.

**이유: 모니터가 하나뿐이라 볼 일이 없다.** 이 앱의 유일한 성공 기준은 "보고 있으면
웃긴가"인데(원칙 1), 사용자가 **한 번도 보지 못할 동작**은 그 기준으로 판정할 수조차 없다.
경계 넘기는 F2 항목 다섯 개와 U1 리뷰가 남긴 숙제 셋을 합쳐 이 프로젝트에서 가장 큰
남은 덩어리였는데, 그 값을 치르고 얻는 것이 **화면에 나타나지 않는다.**

이건 "나중에 모니터가 늘면 하자"와 다르다. 그렇게 두면 F2가 계속 로드맵에 앉아 모션
작업의 순서를 지배한다 — 실제로 "모션을 먼저 만들면 좌표계가 바뀔 때 전부 다시 검증해야
한다"는 이유로 F3 앞에 놓여 있었고, 그 제약이 슬라이딩·발작 같은 창이 움직이는 모션에
**재검증 부채를 계속 달아 놓는다.** 볼 수 없는 기능이 볼 수 있는 기능의 순서를 정하게
두지 않는다.

**이 결정을 뒤집을 조건은 하나다: 모니터가 둘 이상이 되는 것.** 그때 다시 본다.

무엇이 사라지는지:

- F2의 "모니터 경계 넘기", "화면마다 다른 배율·해상도·세로 위치", "빈 공간에 빠지지
  않기", "모니터 연결·해제 대응"이 전부 범위 밖이다. 이 넷은 **경계를 넘기 위해서만**
  존재했다 — 화면 하나에 갇힌 펭귄에게는 물어볼 일이 없는 것들이다.
- PRD Q7(다중 화면 좌표계)의 확정은 **무효**가 된다. 결정 자체가 필요 없어졌다.

무엇이 남는지:

- `pet.rs`의 `World`/`Screen`은 아직 코드에 있다. 다만 **브릿지가 화면 하나짜리
  목록만 만들므로 프로덕션에서는 한 번도 여러 화면을 본 적이 없다** — 다중 화면
  경로는 테스트만 붙들고 있는 죽은 일반화다. 걷어내는 것은 별도 작업으로 남긴다
  (`TODO.md`). 문서에서 먼저 빼는 이유는 **결정과 정리를 같은 PR에 넣으면 되돌릴 때
  어느 쪽이 문제인지 가릴 수 없기** 때문이다.
- 드래그로 다른 화면에 옮기는 것은 그대로다. 창은 이미 커서를 따라가고, 놓는 순간
  그 화면의 경계로 정산된다.

**이 결정을 되돌리려면** 코드가 아니라 이 항목부터 다시 본다. 좌표계 교체(F2 U1)는
이미 머지돼 있어서, 되살리는 비용은 "처음부터"가 아니라 "브릿지가 화면 목록을 실제로
읽게 하는 것"부터다.

### 2026-08-31 (v3.1) — "한 마리"에서 "앱 하나, 마릿수는 사용자가"로

**개정 전 원칙 2**: "펭귄은 한 마리이고, 여러 마리로 늘리지 않는다."
→ 반나절 만에 뒤집혔다. 이유는 **한 마리로 정한 근거가 실제 문제와 어긋났기** 때문이다.
원칙이 막으려던 것은 "화면이 시끄러워지는 것"이었는데, 실제로 겪은 문제는 **앱이 실수로
두 개 떠서 펭귄이 겹쳐 보이는 것**이었다. 그건 마릿수 문제가 아니라 **인스턴스 문제**다.
마릿수를 1로 묶어도 앱이 두 개 뜨면 여전히 두 마리로 보이고, 반대로 앱이 하나라면
사용자가 고른 세 마리는 시끄럽지 않다 — 자기가 부른 것이기 때문이다.
그래서 **사고로 늘어나는 경로는 막고(단일 인스턴스), 골라서 늘리는 경로는 연다(우클릭
추가·삭제).** 원칙이 지키려던 "조용함"은 후자가 아니라 전자가 깨뜨리고 있었다.

### 2026-08-31 (v3.0) — 업무 앱을 그만두고 재미 전용으로

**개정 전 원칙 1**: "Claude Code가 연동의 실행 엔진이다." Notion·Jira·Calendar 조회·기록을
Claude Code에 위임하고, 모델을 작업 성격에 따라 섞어 쓴다.
→ **연동 자체가 사라져서 무효가 됐다.** 위임할 일이 없으면 위임 엔진도 없다. AI를 쓰지
않는다는 v1.0의 원칙이 되돌아왔지만, 이유는 다르다 — v1.0은 비용 때문이었고 v3.0은
**필요가 없기 때문**이다.

**개정 전 원칙 2**: "앱은 런처이고, 깊은 작업은 원래 서비스에서 한다."
→ 런처를 만들다 접었다(`feat/m4-launcher-fan-01`, main에 머지하지 않음). 카드 4장이
펼쳐지는 UI까지 만들어 놓고 보니, 하루에 몇 번 누르자고 펭귄을 거쳐 Chrome을 여는 것보다
Chrome을 그냥 여는 게 빨랐다. **펭귄이 업무 경로 위에 있으면 방해물이 된다** —
업무 경로 밖에 있어야 반가운 존재로 남는다.

**개정 전 원칙 3**: "앱은 업무 데이터를 캐시하지 않는다."
→ 캐시할 업무 데이터가 없어졌다. 원칙 5(방해하지 않는다)의 "설정 말고는 저장하지 않는다"에
흡수됐다.

**개정 전 원칙 4**: "데이터는 원본 서비스에 남긴다."
→ 다루는 데이터가 없다. 무효.

### 2026-08-30 (v2.0) — 앱 내장 통합에서 런처 + Claude Code 위임으로

**개정 전 원칙 1**: "AI를 사용하지 않는다." 요약·캐치·일정 판단을 사용자 수동 조작 +
키워드 규칙으로 해결한다.
→ 근거였던 "별도 비용"은 Claude Code 구독으로 해소된다고 봤다. (v3.0에서 다시 뒤집혔다.)

**개정 전 원칙 2**: "별도 웹사이트를 열고 작업하지 않도록 한다." 브라우저를 열게 만들면
UX는 실패한 것이고, 연동 서비스의 조회·조작은 모두 앱 안에서 끝나야 한다.
→ 정면으로 뒤집혔다. M2에서 Notion TODO 하나의 양방향 UI를 만드는 데 든 비용(플랜 5개,
PR 6개)에 비해 얻은 것이 원래 Notion 웹 UI보다 낫지 않았다.

**개정 전 원칙 3**: "SERVICES가 자동으로 연동되어야 한다" — 주기 검증, 오류 시 전체
재동기화, 쓰기 재시도 큐.
→ 앱이 캐시를 들지 않으므로 장치 자체가 불필요해졌다.

**이 두 번의 개정에서 배운 것**: 만들기 전에는 어느 쪽이 나은지 몰랐고, **만들어 보고서야
알았다.** M2를 만들어 보고 v2.0으로, M4를 만들어 보고 v3.0으로 왔다. 그러니 다음에도
"이걸 만들어야 아는가"를 먼저 묻고, 만들어야 안다면 **가장 작게** 만들어 본다.
