# TODO

> [PRD.md](PRD.md) §10 마일스톤 기준. 진행 상황의 단일 원천이다.
>
> **2026-08-31 방향 전환 (v3.0)** — 업무 기능을 **전부** 뺐다. 뽀모도로, 서비스 런처,
> Claude Code 위임, Notion·Jira·Calendar 연동이 모두 범위 밖이다. 남는 것은
> **바탕화면을 돌아다니는 펭귄 한 마리**뿐이고, 목표는 쓸모가 아니라 **재미**다.
> 근거는 [PRINCIPLE.md](PRINCIPLE.md) 개정 이력.

## 완료 — 던지기 속도

- [x] **던지기가 너무 빠르다** — 상한이 화면 크기와 무관한 고정값(`THROW_MAX_SPEED = 2600px/s`)
      이라 1440px 화면을 0.55초에 가로질렀다. 상한을 **초당 세계를 0.9번 가로지르는
      속도**로 바꿔(세계 = 펭귄이 다닐 수 있는 가로 범위), 화면이 넓어지면 같은 손짓이
      더 멀리 가게 했다. 던짐/떨어뜨림 문턱은 손의 의도에
      대한 것이라 고정으로 남겼다 (2026-08-31)

## 펭귄 마릿수 ← 여기부터

> **앱이 실수로 두 개 뜨는 것**과 **사용자가 펭귄을 늘리는 것**은 다른 문제다.
> 앞은 막고 뒤는 연다 (PRINCIPLE 2, 2026-08-31 개정).

- [x] **앱 인스턴스를 하나로 강제** — 가드가 아예 없어서 두 번 실행하면 그대로 두 개가
      떴다. `tauri-plugin-single-instance`로 두 번째 실행이 스스로 물러나고, 이미 떠 있는
      인스턴스가 팝오버를 열어 알린다 (2026-08-31). **Rust 전용 플러그인이라 capabilities 등록은 필요 없다**
      (Cargo.toml + `lib.rs`의 `.plugin(...)` 두 곳). 등록은 다른 플러그인보다 **먼저** 한다
- [x] **우클릭으로 펭귄 추가·삭제** — 상태를 `Pets` 컬렉션으로, 창 라벨을 `pet-<id>`로 열었다.
      커맨드는 **호출한 창의 라벨**로 대상을 정하고(웹뷰가 보내는 id는 믿지 않는다), 마리마다
      다른 시드를 받고, 마릿수는 저장된다. 마지막 한 마리는 삭제 불가, 상한 8마리 (2026-08-31)

## 던지기·모션 손보기

- [x] **착지를 세기에 따라 나눈다** — 낙하 속도로 네 갈래. 150 미만은 그냥 서고,
      150~700은 **통통 튀고**(진짜 반동), 700~1000은 **철푸덕**(850ms), 1000 이상은
      **널브러짐**(1700ms). 착지 포즈는 `<g class="pg-all">`로 본체를 통째로 누른다 —
      부위별로 누르면 머리가 공중에 뜬다 (2026-08-31)

## F1 — 걷어내기

> 타이머를 안고 가면 이후 모든 리팩터링이 두 배가 된다. 제거가 가장 확실한 작업이라 먼저 한다.
> 끝나면 앱에 남는 것은 펭귄 창 + 설정 창 둘뿐이다.

- [x] 뽀모도로 타이머 제거 — `pomodoro.rs`·`timer_bridge.rs`, `TimerCard`, `lib/timer.ts`,
      커맨드 7개, 저장소 키, 타이머 CSS까지. `now_ms()`는 `pet_bridge`로, `TRAY_ID`는
      `lib.rs`로 옮겼다 — 펭귄과 트레이가 타이머 모듈에 얹혀 있었다 (2026-08-31)
- [x] 알림 제거 — `lib/notification.ts`와 `tauri-plugin-notification`을 네 곳 모두에서
      뺐다 (Cargo.toml · package.json · `lib.rs` · capabilities). 앱은 이제 알림을
      하나도 보내지 않는다 (2026-08-31)
- [x] 팝오버를 설정 창으로 정리 — 마릿수·대사·on/off 셋만 남았다. `SettingsCard`에서
      시간 설정을 걷어내 펭귄 토글만 남겼다 (2026-08-31)
- [x] 소리 on/off 설정 추가 — **기본 꺼짐** (PRD Q6). 낼 소리는 F3에서 붙이고 지금은
      값과 저장만 있다. 켰는데 조용하면 고장으로 읽히므로 "아직 낼 소리가 없어요"를
      옆에 적었다. 겸사겸사 `savePetSettings`가 같은 키의 마릿수를 덮어쓰던 것도 고쳤다 (2026-08-31)
- [x] `feat/m4-launcher-fan-01` 브랜치 폐기 — 머지하지 않고 원격 브랜치를 삭제했다 (2026-08-31)

## ~~F2 — 세계 넓히기~~ (2026-08-31 폐기)

> **모니터 경계 넘기를 범위 밖으로 뺐다.** 접은 게 아니라 아예 제외다 —
> **모니터가 하나뿐이라 볼 일이 없다.** 볼 수 없는 기능이 볼 수 있는 기능의 순서를
> 정하게 두지 않는다 (F2는 "좌표계를 먼저 바꿔야 한다"는 이유로 F3 앞에 있었다).
> 근거와 되돌리는 비용은 [PRINCIPLE.md](PRINCIPLE.md) 개정 이력(v3.2)에 있다.
> PRD §5.2와 Q7도 같이 폐기했다.
>
> 아래 넷은 **경계를 넘기 위해서만** 있던 항목이라 함께 사라진다 — 화면 하나에 갇힌
> 펭귄에게는 물어볼 일이 없다.
>
> - ~~모니터 경계 넘기~~ (U1 리뷰가 남긴 숙제 셋도 함께 사라진다)
> - ~~화면마다 다른 배율·해상도·세로 위치 처리~~
> - ~~빈 공간에 빠지지 않기~~
> - ~~모니터 연결·해제·배치 변경 대응~~
> - ~~설정 창을 펭귄이 있는 화면에서 열기~~ — 화면이 하나면 물어볼 일이 없다
>
> 후속 목록에 있던 다음 둘도 같이 지웠다 — 경계를 넘지 않으면 성립하지 않는다.
>
> - ~~화면 사이 빈 공간을 "떨어져 죽는 구멍"처럼 연출하기~~
> - ~~세로로 쌓은 배치에서 위 화면으로 날아 올라가기~~

이미 머지된 둘은 **그대로 남는다.** 되돌리지 않는다 — 동작은 화면 하나짜리 세계에서
바뀐 게 없고, 되돌리는 diff가 오히려 회귀 위험이다.

- [x] **PRD Q7 결정** — 화면별 `Bounds` 목록으로 정했었다. **v3.2에서 무효** — 경계 넘기를
      빼면서 물어볼 일이 없어졌다 (2026-08-31)
- [x] 코어(`pet.rs`)를 다중 화면 좌표계로 — `Bounds` 하나를 받던 자리를 `World`(화면 목록)로
      바꿨다. **브릿지가 화면 하나짜리 목록만 만들므로 프로덕션에서는 한 번도 여러 화면을
      본 적이 없다** (2026-08-31)

- [ ] **쓰지 않게 된 다중 화면 좌표계를 걷어낼지 정한다** — 경계 넘기가 빠지면서
      `World`/`Screen`의 여러 화면 경로(`screen_at`·`nearest`·`screen_for_x`·`anchor_area`)는
      **테스트만 붙들고 있는 죽은 일반화**가 됐다. 브릿지는 `World::single`만 만들고
      `available_monitors()`를 한 번도 부르지 않는다. 걷어내면 `step`이 다시 `Bounds`
      하나를 받고 코어가 그만큼 얇아지지만, **모든 모션의 시그니처와 테스트를 건드리는
      기계적 대공사**라 회귀 위험이 있다. 지금 결정하지 않고 항목으로 남긴다

- [x] **모니터를 뽑으면 펭귄을 잃어버리던 것** — 확장 모니터 선을 뽑았더니 두 마리가
      사라져 다시는 안 보였다. 20Hz 틱이 경계를 2초마다 다시 읽는데, **읽기에 실패하면
      캐시를 갱신하지 않고 넘어갔다.** 창이 어떤 화면에도 안 걸치면
      `current_monitor()`가 `None`을 주므로, 모니터를 뽑는 순간이 정확히 그 경우다 —
      사라진 화면의 경계가 캐시에 영원히 남아 펭귄이 매 틱 그 좌표로 clamp됐다.
      상태에도 있고 창도 살아 있는데 갈 방법이 없었다. 못 읽으면 **주 모니터로
      떨어지게** 했다(`world_to_cache`) — 그러면 `clamp`가 다음 틱에 데려온다.
      **F2를 되살린 게 아니다**: 경계를 넘어 다니게 하는 것은 여전히 범위 밖이고,
      이건 "잃어버리지 않는다"는 결함 수정이다 (2026-09-01)

## F3 — 모션 늘리기

> 카탈로그와 각 동작의 의도는 [MOTIONS.md](MOTIONS.md)에 있다. 하나씩 별도 PR로 간다.

- [x] **벽에서 굴러떨어지기** — 벽에 닿으면 늘 얌전히 돌아서던 것을 확률로 갈랐다.
      30%는 그대로 박고 뒤로 나자빠진 뒤 일어난다. 새 동작을 하나 더 만드는 것보다
      **이미 매일 일어나는 사건**에 갈래를 주는 게 체감 밀도를 더 올린다. 굴림은
      좌우 경계가 공유하는 한 곳에서 하고, 감속은 남은 시간 비율로 해서 **끝나는
      순간 속도가 0**이 되게 했다(마찰 상수를 두면 영원히 미끄러지는 상태가 생긴다).
      나가는 길은 착지와 공유한다 (2026-08-31)
- [x] **얼음낚시** — 걷기·유휴가 끝났을 때 0.7%로 시작해 **항상 30~60초**를 앉아 있는다.
      국면 여섯(구멍 뚫기·드리우기·입질·잡음·꽝·정리)을 돌고, 잡아도 다시 드리운다 —
      **판을 끝내는 것은 예산뿐이다.** 잡으면 끝내게 했더니 중앙값이 18.6초로 내려가
      졸기보다 짧아졌다. 국면과 잡음/꽝은 코어가 정한다("무슨 동작"이라 웹뷰가 뽑으면
      같은 시드가 같은 결과를 내지 않는다). 예산은 절대 시각 하나로 갖고 **드리우기에
      들어갈 때만** 확인한다. **창 여백은 늘리지 않았다** — 잘림의 경계는 `viewBox`가
      아니라 창이다. 헤엄치는 중에 시키면 **허공에서** 낚시한다 (2026-08-31)
- [x] **설정 창에 "동작 시켜보기" 카드** — 십 분에 한 번짜리 동작을 기다려서는 확인할 수
      없다. 우클릭해서 연 **그 펭귄**만 대상이고(`pet_remove`와 같은 규칙), 끝나는 조건
      ("던지거나 때리면 그만둔다")을 누르기 전에 적는다. 슬라이딩 등이 생기면 버튼만
      늘린다. 겸사겸사 팝오버가 스크롤 위치를 기억해 맨 위 카드가 사라진 것처럼 보이던
      것도 고쳤다 — 창은 닫을 때 파괴되지 않고 숨겨질 뿐이다 (2026-08-31)
- [x] **슬라이딩** — 걷기가 끝났을 때 20%로 배를 깔고 미끄러진다. 유휴 뒤에는 안 나온다
      (서 있다가 갑자기 배를 깔면 준비 동작이 없다). 감속은 남은 시간 비율로 하고
      (마찰 상수를 두면 영원히 미끄러지는 상태가 생긴다), **길이는 고정하고 출발 속도를
      뽑는다** — 길이를 뽑으면 CSS 길이를 코어 상수와 맞출 수 없다. 속도 하한은 계산이다:
      가장 느린 슬라이딩(264px)이 가장 긴 걷기(252px)보다 멀어야 한다. 벽 반응은 걷기와
      같은 `hit_wall`을 쓴다 (2026-08-31)
- [x] **빽빽거리기** — 2초 안쪽 간격으로 **스무 번 연달아** 맞으면 스윙을 건너뛰고
      곧바로 부풀리고 퍼덕이며 화낸다(1.4초). 스윙 뒤로 미루면 연타 중에는 매 클릭이
      스윙을 다시 걸어 **연타를 멈춘 뒤에야** 터진다 — 반응이 자극에서 떨어지면 자기
      손짓과 연결이 안 된다. **빽빽거리는 중에 또 맞으면 판을 새로 연다** — 스윙으로
      끊으면 화가 보일 시간이 없고, 남은 시간만 주면 웹뷰가 애니메이션을 되감았다가
      끊겨 영원히 부풀기만 한다. 때리는 동안 계속 화내고 손을 떼면 1.4초 뒤에 끝난다.
      나가는 길은 `get_up`이 아니라 유휴다 — 화를 다 낸 직후에 약을 올리면 화가
      연기로 보인다. **기본 무음** (2026-08-31)
- [ ] **빽빽거리기 자발 트리거** — "낮은 확률로 혼자서"를 범위에서 뺐다. **화에는
      원인이 있어야 읽히고**, 이유 없이 터지는 동작을 둘 만들면 발작의 희귀함이 먼저
      희석된다. **발작을 만들 때 함께 본다** — 이유 없이 터지는 동작의 총량 문제다
- [x] **발작** — **30,000번에 한 번**(한 사이클 4초 기준 깨어 있는 33시간, 하루 열 시간이면
      사흘에 한 번꼴) 이유 없이 터진다. **PRD Q8을 "순수 저확률 무작위"로 닫았다** —
      연타는 빽빽거리기가 가져갔고, 무엇보다 "아무 일 없었다는 듯 돌아온다"는
      **원인이 없어야 성립한다**. 던지기 물리 대신 헤엄식 목적지를 쓴 이유는 중력이
      바닥으로 끌고 가면 착지 판정이 철푸덕·널브러짐을 유발하기 때문이다. 국면 둘
      (돌진·숨 고르기)로 나눠 CSS 길이 대조와 자세 튐을 함께 풀었다. 골든 수열
      **네 번째 재기준화** (2026-09-01)
- [x] **빈도 설계 재조정** — 추측 대신 **쟀다**(시드 5개 × 4시간, `pet.rs`의
      `빈도_측정` 테스트). 등급 순서는 멀쩡했지만 **적어 둔 숫자가 최대 3배 틀렸다** —
      원인이 하나다: 얼음낚시·발작 확률을 정할 때 쓴 "걷기·유휴 한 사이클 4초"가
      실제로는 **6.8초**다(벽에 닿아 끝난 걷기는 `hit_wall`로 빠져 `pick_next`를
      안 거친다). 얼음낚시 10분→16분, 발작 33시간→57시간, 슬라이딩 30초→100초,
      `Turn`은 기본이 아니라 82초에 한 번이다. **값은 그대로 두고 문서를 실측에
      맞췄다.** 등급이 조용히 뒤집히는 것은 `빈도_등급이_순서대로다`가 막는다 (2026-09-01)

- [x] **헤엄이 끝날 때마다 자유낙하해서 널브러지던 것** — 20시간 동안의 착지가
      **전부** 헤엄 끝에서 나왔고 그중 78%가 철푸덕(52회/h)이나 널브러짐(62회/h)
      이었다. "세게 던진 보람"으로 만든 동작이 1분에 한 번씩 저절로 나오면 보람이
      남지 않는다. 헤엄이 도착하면 **날개를 저어 바닥까지 내려앉고** 통, 하고
      닿게(`Land`) 바꿔 **저절로 생기는 철푸덕·널브러짐을 0으로** 만들었다
      (던지기만이 아니라 헤엄 중 클릭으로도 나온다 — 사용자가 만든 것은 그대로다).
      내려앉는 속도는 헤엄보다 빠르게 따로 뒀다(`SWIM_DESCENT_SPEED`) — 같은
      속도면 꼭대기에서 바닥까지 8.4초라 헤엄 화면 점유가 27.8% → 42.2%로 뛴다.
      착지를 고치려던 변경이 "절반쯤 공중에 떠 있는 앱"을 만들면 안 된다.
      골든 수열 **다섯 번째 재기준화** — 갈래가 는 게 아니라 뒤에 오는 동작이
      달라져 난수 소비 시점이 밀린 첫 경우다 (2026-09-01).
      **아래 항목에서 확률로 되돌렸다** — 이 문단만 읽고 "저절로는 안 떨어진다"로
      알면 안 된다
- [x] **헤엄이 끝나면 다시 떨어진다 — 90%** — 위 변경이 널브러짐을 0으로 만들면서
      **하늘에서 떨어지는 그림도 함께 없앴다.** 사용자가 그쪽을 더 아쉬워해서
      확률로 되돌렸다(지시). `SWIM_FREEFALL_PERCENT` 90 — 나머지 10%는 그대로
      날개를 저어 내려앉는다. **100이 아닌 이유는 갈래**다: 헤엄의 끝이 하나뿐이면
      다시 예측 가능한 지점이 된다. 대가는 재서 문서에 적었다 — 널브러짐 55회/h,
      철푸덕 48회/h가 저절로 나오고 그 전부가 헤엄 끝에서 온다. 헤엄 화면 점유는
      37.1% → **28.7%**로 내려와 내려앉기 이전(27.8%)과 거의 같아졌다.
      **이미 내려앉는 중이면 다시 추첨하지 않는다** — 예산 만료로 그 분기에 다시
      들어오는데, 거기서 뽑으면 난수를 태우고 절반쯤 내려온 펭귄이 갑자기 샌다.
      골든 수열 **여섯 번째 재기준화** (2026-09-01)
- [x] **효과음** — 켠 경우에만(기본 꺼짐), **넷에서만** 난다(당시 기준 — 뒤에 아래
      "잡았다" 예외가 붙어 다섯이 됐다): 빠따 맞기(퍽)·날아가기(휙)·
      빽빽거리기(빽!)·발작(광란). 기준은 "사용자가 방금 한 짓의 결과이거나, 시간당 한 번보다
      드물거나" — 착지 셋은 시간당 153회가 저절로 나서 뺐다 (`MOTIONS.md` 효과음 절).
      **Q9는 직접 합성(Web Audio)으로 확정** — 음원 파일이 없어 번들이 안 늘고 라이선스
      표기가 없으며, 마리마다 목소리 높이가 다르다(라벨에서 결정적 파생). 퍽은 스윙이
      아니라 `whack_seq`에 붙어 핀볼 채 타격에서는 저절로 빠진다. **Rust 무변경** —
      소리는 "어떻게 들리는지"라 웹뷰 소유다. **음량 다섯 단계**(기본의 ¼~4배)도
      사용자 지시로 함께 넣었다 — "슬라이더 없음" 결정(KTD9)을 뒤집은 것 (2026-09-01)
- [x] **핀볼 모드 (on/off)** — 착지 등급 판정을 우회하고 벽·천장·바닥이 전부
      반사면(0.92)이 된다. 500px 낙하에서 스물두 번 튄다. **1.0으로 두지 않았다** —
      영원히 튀면 펭귄이 다시는 걷지 않고 20Hz 틱도 안 쉰다. 아래 문턱
      (`BOUNCE_MIN_SPEED`)을 남긴 것도 같은 이유다.
      **미니게임 형태로 넣었다**(사용자 요청): 왼쪽 클릭이 빠따가 아니라 **채**가 되어
      맞은 지점의 반대 방향으로 펭귄이 날아가고, 커서가 방망이로 바뀐다. 창이 펭귄
      크기라 커서가 바뀌는 범위가 곧 칠 수 있는 범위다. **연타 빽빽거리기는 건너뛴다** —
      스무 번 치는 게 정상적인 랠리인데 거기서 멈춰 화내면 판이 끊긴다.
      **점수·목숨·판은 넣지 않았다** — PRD §4의 게임화 비목표를 건드리지 않는다.
      `landing_of`를 지우지 않고 `Pet::landing`이 앞에서 가로채므로 **끄면 그대로
      돌아온다.** 플래그는 `step`의 인자가 아니라 필드다 — 인자로 받으면 모든 모션
      테스트를 건드리는 대공사가 된다 (2026-09-01)

## 볼링 — 여러 마리가 함께 ← 여기부터

> 플랜: [`docs/plans/2026-09-02-021-feat-bowling-plan.md`](docs/plans/2026-09-02-021-feat-bowling-plan.md)
>
> **PR 셋으로 나눈다.** 첫째(이음매)가 나머지 둘의 공통 선행이고, 그게 머지되면
> 둘째·셋째는 **병렬로** 갈 수 있다 (2026-09-02 사용자 지시). 병렬로 돌릴 때는
> worktree로 나눈다 — 한 레포에서 브랜치를 오가면 두 러너 캐시가 계속 무효화되고,
> 한쪽에서 골든 수열을 재기준화하는 중에 넘어가면 뭐가 깨진 건지 분간이 안 된다.

- [x] **펭귄들이 서로를 볼 수 있게 한다 (이음매)** — 마리별 `step` 루프가 브릿지에
      있어서 **한 마리의 `step`이 다른 마리를 볼 방법이 코어에도 브릿지에도 없었다.**
      루프를 `Pets::step_all`로 끌어올렸다. **`Pet::step`의 시그니처는 안 건드린다** —
      다른 마리를 인자로 넘기면 모든 모션 함수와 테스트를 고치는 대공사가 된다
      (핀볼 플래그를 인자가 아니라 필드로 둔 것과 같은 이유). 락도 마리마다가 아니라
      **틱마다 한 번** 잡는다 — 틱 하나가 전 마리에 원자적이어야 서로를 보는 판정을
      여기 얹을 수 있다. 창을 찾고 경계를 읽는 일은 코어 밖에 남겼다.
      **합격 기준은 동작 불변**이었다: 기존 테스트 파일을 **한 줄도 고치지 않고**
      전부 통과, 그리고 `여러_마리를_한_번에_돌려도_따로_돌린_것과_같다`가 60초 치를
      대조한다. **소비자가 요구하는 것만 담았다** — 읽기 패스도 위치 조회 API도 아직
      없다. 설계되지 않은 기능을 위한 확장점을 미리 파는 것이 v1.0·v2.0을 만든
      논리다 (PRINCIPLE 1) (2026-09-02)
- [ ] **볼링** — 펭귄들이 걸어가 화면 오른쪽에 한 줄로 서고, 다 서면 왼쪽에 놓인 공을
      마우스로 집어 **수평으로** 굴린다. 세기는 드래그가 정하고, 공이 지나간 펭귄은
      빙글빙글 돈다. 공이 멎으면 판이 끝난다. **점수·프레임·스트라이크는 없다.**
      플랜의 U2~U9이고 위 이음매 위에서 시작한다. 플랜을 쓰면서 착수 시 합의를 **두 번
      뒤집었다**: (a) 핀볼 판(`pinball-board-*`)을 재사용하지 않는다 — 볼링은 사용자를
      가두지 않으므로 화면을 덮을 이유가 없고, 공은 **자기 창**을 가져 펭귄 창의 드래그
      패턴을 통째로 재사용한다. (b) 핀은 삼각형이 아니라 **한 줄**이다 — 펭귄은 전부
      `floor_y` 위에 서고 2차원 바닥에는 깊이가 없다. PRD는 §5.9 신설만 하고 §4·§5.8은
      고치지 않는다 (근거는 플랜 KTD9)
- [ ] **핀볼 모드에서 마리끼리 부딪히기** — 위 이음매가 막고 있던 항목이다. 이제
      `Pets::step_all`에서 전 마리 위치를 훑을 수 있으므로 **볼링과 병렬로** 갈 수 있다.
      여덟 마리가 동시에 튀는데 통과해 지나가는 게 어색한지부터 보고 정한다.
      **착수할 때 `tick.rs`의 3단계 주석에 적힌 회수 조건을 먼저 확인한다** — 마리 간
      판정이 들어와 `step_all`이 무거워지면 락 보유 시간과 `apply` 지연이 함께 늘어난다

## 후속 (급하지 않음)

- [ ] **저절로 생긴 낙하의 착지 등급에 상한을 둘지** — 헤엄 끝 자유낙하를 90%로
      되돌리면서 널브러짐 55회/h·철푸덕 48회/h가 저절로 나온다. 거슬리면 갈래는
      둘이다: (a) `SWIM_FREEFALL_PERCENT`를 낮춘다, (b) **저절로 생긴 낙하는
      통통까지만** 허용해 떨어지는 그림은 남기고 널브러짐만 없앤다. 지금은
      제한 없이 뒀다 — 얼마나 거슬리는지 보고 정한다
- [x] **얼음낚시 "잡았다" 효과음** — 자격 규칙의 **첫 예외**로 넣었다(2026-09-01 사용자
      지시). 근거를 따로 썼다: 규칙이 막으려던 것은 배경 소음의 누적인데 잡음 8회/h는
      16분에 한 번 열리는 판 안에만 몰려 있고, 잡는 순간은 서사의 보상 지점이다. 대신
      소리를 다른 것보다 작고 짧게(첨벙 물소리, ≈350ms) 잡았고 **꽝은 계속 무음이다** (2026-09-01)
- [ ] **합성음이 "펭귄"으로 안 들리면 음원 파일 재검토** — Q9 확정(직접 합성)의 탈출구.
      써 보고 판단한다
- [ ] **빠따 연타가 되감기지 않는다** — `pg--swing`이 `isOneShot` 목록에 없어서, 360ms
      안에 연타하면 방망이가 다시 안 휘둘러진다. 클래스가 그대로라 CSS 애니메이션이
      재생되지 않는다. 고치려면 `shouldRestart`에 구분자(`whack_seq`)를 함께 넘겨야 한다
- [x] **핀볼 판 — 켠 동안 화면 전체가 판이 된다** — #38의 커서는 펭귄 창 위에서만
      방망이였다(커서 모양은 그 밑의 창이 정한다). 화면 전체를 덮는 투명 창을 깔아
      **어디서나 방망이**로 만들고, 바탕화면 클릭을 먹게 했다.
      **판은 펭귄 창보다 아래에 깐다** — 위에 두면 클릭·드래그를 화면 좌표로 다시
      짜야 하는데, 아래에 두면 펭귄 상호작용이 한 줄도 안 바뀐다.
      **나가는 문이 둘이다**(Esc·트레이). 트레이가 문으로 성립하는 근거는 macOS의
      창 레벨 규칙이라 앱이 망가져도 남는다. 켜기가 실패하면 모드가 스스로 되돌아온다.
      **판은 화면마다 하나씩**이다 — 창 하나로 합쳐 덮으면 배율이 다른 화면에서
      어긋난다. **판의 창 레벨을 펭귄보다 낮춘다** — 같은 레벨 안의 순서는 클릭할
      때마다 바뀌어서 한 번 올려 두는 방식은 전부 실패했다
      (`docs/solutions/ui-bugs/macos-window-order-is-not-stable-level-is.md`).
      방망이는 `:active`로 프레임을 갈아 끼워 누르면 위로, 떼면 아래로 간다 —
      커서 이미지에는 애니메이션이 안 걸린다 (2026-09-01)
- [ ] **판이 클릭을 받아 방망이를 그릴지** — 지금은 커서 이미지 두 장이라 호를 그리며
      도는 진짜 스윙이 아니다. 그러려면 판을 펭귄 위로 올려 클릭을 다 받아야 하고,
      그러면 히트 판정과 드래그를 화면 좌표로 다시 짜야 한다. 두 프레임으로 충분한지
      먼저 써 보고 정한다
- [ ] **모니터가 바뀌면 판 크기 갱신** — 지금은 켤 때 한 번 잰다
- [x] **흑백 템플릿 트레이 아이콘** — 다크/라이트 메뉴바 자동 적응. 실루엣은 새로 그리지
      않고 기존 `tray.png`의 알파 채널을 그대로 두고 색만 전부 검정으로 바꿨다(모양 논의를
      안 열기 위해). 코드는 `icon_as_template(true)` 한 줄 (2026-09-01)
- [x] **테마 설정 — 시스템/라이트/다크** (2026-09-01 사용자 지시, 위 템플릿 아이콘의 연장) —
      설정 창 겉모습을 정한다(`app.set_theme` — 웹뷰 prefers-color-scheme이 뒤집혀 CSS
      추가 없이 됨). 시작 시 Rust가 저장값을 읽어 건다(`theme_from` — 깨진 값은 시스템으로).
      **트레이는 테마에서 뺐다**(리뷰 지적 반영): 메뉴바 색은 OS만 정해서 실루엣을 고정하면
      시스템과 다를 때 안 보인다 — 항상 템플릿(자동)이다 (2026-09-01)
- [ ] **CSS 길이와 Rust 상수의 대조 범위를 넓힐지** — `pet-css.test.ts`가 대조하는 것은
      `animation` 78개 중 9개뿐이다. 나머지는 어긋나도 아무것도 실패하지 않는다
- [ ] **사용자가 모션을 만들어 공유하는 기능** — 넣을지 말지부터 PRD로 정해야 한다.
      "공유"는 PRD v3.0의 "사용자는 본인 1명, 배포 없음"과 PRINCIPLE 5의 "설정 말고는
      저장하지 않는다"를 동시에 건드린다. 만들기로 하면 `behavior.rs`가 적어 둔 **일곱 자리를
      전부 데이터로** 바꾸는 별도 설계다. **확장점을 미리 파 두지 않았다** — 설계되지 않은
      기능을 위해 추상화를 세우는 것이 v1.0·v2.0을 만든 논리다 (PRINCIPLE 1)
- [ ] **핀볼 판 켜기가 도중에 실패하면 앞서 만든 판이 남는다** — `create_pinball_window`는
      모니터를 순회하는데, 둘째 모니터에서 실패하면 첫째 판 창이 그대로 남은 채
      상태만 꺼진다. 화면에는 투명 판이 깔려 있는데 상태는 "꺼짐"이라 나가는 문이
      안 보인다. 실패 반환 전에 `close_pinball_window`를 부르면 된다.
      **`main`에도 있던 문제다** (2026-09-02 CodeRabbit 지적, 리팩토링 PR 범위 밖이라 미룸)
- [ ] **`prefers-reduced-motion`이 transition을 안 끈다** — `animation: none !important`만
      걸어서 `.penguin`의 `transform 0.25s` 전환은 그대로 돈다. 움직임 감소를 켠
      사용자에게 헤엄 방향 전환이 여전히 회전한다. **`main`에도 있던 문제다** (2026-09-02)
- [ ] **`pg--sassy-butt-wiggle`이 `SASSY_MS`보다 10ms 길다** — `0.7s × 1.3` = 910ms인데
      상태는 900ms다. 반복이 정수가 아니라 keyframe 30% 지점에서 잘려 몸 회전이
      중간 자세로 되돌아온다. `0.45s × 2`면 정확히 900ms에 정수로 끝난다.
      **위의 "CSS 대조 범위를 넓힐지" 항목이 잡았어야 할 부류다** — 대조가 9개뿐이라
      놓쳤고 CodeRabbit이 찾았다. **`main`에도 있던 문제다** (2026-09-02)
- [ ] 로그인 시 자동 실행 (autostart)
- [ ] 모션 더 — 점프, 화면 가장자리에 기대기, 창 위에 올라앉기, 커서 쫓아다니기
- [ ] 펭귄이 다른 창에 반응하기 (창 모서리를 바닥으로 삼아 걷기)
- [x] **`pet.rs`가 4616줄까지 갔다 — 도메인으로 쪼갰다** (2026-09-02). `world.rs`·`motion.rs`
      두 파일이 아니라 **동작 하나가 파일 하나**다 — `motion/{ground,air,react,drag,pinball,
      fishing,freakout}.rs`에 매 틱 물리·진입·퇴장·테스트가 함께 있다. `motion.rs` 한 파일로
      묶지 않은 이유는 그 이름이 "모션은 여기 다 있다"는 거짓말을 하기 때문이다 — 모션 하나는
      **일곱 자리**에 흩어져 있고 목록은 `behavior.rs` 모듈 문서에 있다.
      **테스트 블록에 손으로 그어 둔 `// ── 얼음낚시 ──` 구분선이 근거였다** — 작성자는 이미
      도메인으로 생각하고 있었고 파일이 그걸 못 담고 있었다. `step`이냐 `whack`이냐 하는
      기계 기준으로 나누면 "얼음낚시가 어디 있나"에 답할 수 없다.
      상수는 `tuning.rs` 한 곳, 좌표계는 `world.rs`, `pick_next`(추첨기)·`enter`·`clamp`·난수는
      `mod.rs`에 남겼다 — 확률이 흩어지면 서로의 비중이 안 보인다.
      **합격 기준은 테스트 이름 다중집합 불변**(206개)이었다. 매 커밋 대조했다.
      같은 PR에서 `pet_bridge.rs`(1643줄)와 `pet.css`(1693줄)도 쪼개고, 테스트를 전부
      별도 파일로 떼고, 주석을 WHAT만 남기고, `field`를 `pinball`로 이름 바꿨다 (사용자 지시).
      최대 파일이 4616 → 445줄이 됐다
- [ ] 굴러떨어질 확률을 설정에 노출할지 — 지금은 상수(`TUMBLE_AT_WALL_PERCENT`)다
- [ ] 굴러떨어지는 세기를 벽에 닿은 속도로 나눌지 — 지금 걷기 속도가 하나뿐이라
      의미가 없다. **슬라이딩이 들어오면** 다시 볼 값어치가 생긴다
- [ ] 천장·바닥 모서리에서의 반응 — 굴러떨어지기는 좌우 벽만 다룬다

---

## 닫힌 마일스톤 (v1.0 · v2.0)

> 지나온 이력이다. **다시 열지 않는다** — 왜 접었는지는 [PRINCIPLE.md](PRINCIPLE.md)
> 개정 이력에 있고, 읽지 않으면 같은 논의를 처음부터 다시 하게 된다.

### 완료

- [x] 기술 스택 선택 → **Tauri v2 + React/TS** (PRD Q1, 2026-08-06)
- [x] 개발 워크플로우 정비 — `CLAUDE.md` + `develop` 스킬 (2026-08-09)
- [x] **M1** 메뉴바 상주 앱 골격 + 카드형 팝오버 + 뽀모도로 타이머 + 알림 (2026-08-06~09)
      → 타이머·알림은 **F1에서 제거**
- [x] **M2** Notion TODO 통합 — 연결·조회·추가·토글·편집·수행도 (2026-08-09~10)
      → v2.0에서 제거 완료
- [x] **M2.5** 바탕화면 펭귄 — 투명 창, Rust 20Hz 틱 코어(`pet.rs`), 이동·유휴 동작,
      클릭 빠따와 싸가지 반응 5종, 드래그 던지기 포물선, 킹받는 한마디와 대사 편집,
      시선 추적, on/off (2026-08-30, PR #11·#16·#17·#18) → **v3.0의 전부가 여기서 이어진다**
- [x] **M3** Notion 기능 제거 + 의존성 정리 (2026-08-30)

### 폐기

- [x] ~~**M4** 런처 (펭귄 우클릭 → 카드 4장 → Chrome)~~ — `feat/m4-launcher-fan-01`에
      만들어 놓고 **머지하지 않았다.** 하루에 몇 번 누르자고 펭귄을 거치는 것보다
      Chrome을 그냥 여는 게 빨랐다 (PRINCIPLE 개정 이력)
- [x] ~~**M5** 뽀모도로 ↔ 펭귄 연동~~ — 타이머 자체가 사라졌다
- [x] ~~**M6** Claude Code 위임 (Q3 미정 상태)~~ — 위임할 연동이 없다
- [x] ~~토큰 보관 방식 (Q2, macOS Keychain)~~ — 보관할 토큰이 없다
