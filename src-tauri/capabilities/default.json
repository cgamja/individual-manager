{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and pet windows",
  "windows": ["main", "pet"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "opener:default",
    "store:default",
    "notification:default"
  ]
}
