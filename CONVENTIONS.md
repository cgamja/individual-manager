# 지켜야 할 컨벤션

> 관련 문서: [PRD.md](PRD.md), [TODO.md](TODO.md), PR 템플릿: [.github/TEMPLATE/PR.md](.github/TEMPLATE/PR.md)
>
> **2026-08-31 (v3.0)** — 앱이 외부 서비스를 부르지 않게 되면서 "외부 API 연동 테스트"와
> "시크릿 관리" 항목을 실제 범위에 맞게 줄였다.

### 커밋 컨벤션 — Angular Convention
- 타입: `feat`, `fix`, `refactor`, `chore`, `docs` 사용 (필요 시 `test` 추가 가능)
- 언어: 한국어
- 제목: 50자 이내, `타입: 제목` 형식 (ex. `feat: 뽀모도로 타이머 종료 알림 추가`)
- 본문: 한 줄 72자 이내. 무엇을/왜 바꿨는지 위주로 작성
- 커밋은 기능 단위 별로 나누기. 너무 짤막하지 않게 (파일 하나 오타 수정 수준으로 쪼개지 말고, 하나의 의미 있는 변경을 하나의 커밋으로)

### TDD 적용 및 테스트 생성
- 기능 구현 전 실패하는 테스트를 먼저 작성한다 (Red → Green → Refactor)
- 테스트 우선순위:
  1. **핵심 로직** (`pet.rs`의 동작 전이·화면 경계 판정·던지기 물리·모션 추첨): 반드시 단위 테스트 작성.
     Tauri에 의존하지 않는 순수 모듈로 두고, 난수는 **시드를 받아** 같은 입력에 같은 결과가 나오게 한다
  2. **브릿지** (`pet_bridge.rs`의 좌표 변환·화면 목록 처리): 순수 함수로 떼어낼 수 있는 부분은 테스트한다
  3. **UI·CSS 모션**: 필수 아님. 수동 확인으로 대체 가능
- 테스트 이름은 한국어로 동작을 설명 (ex. `공중에서_클릭하면_제자리에서_반응한다`)
- PR을 올리기 전 전체 테스트가 통과해야 한다

### 브랜치 전략
- 각 기능을 만들 때 branch를 파서 작업한다
- 브랜치 이름: `타입/기능-설명-번호` (ex. `feat/project-setting-06`)
- 타입은 커밋 타입과 동일하게 사용 (`feat`, `fix`, `refactor`, `chore`, `docs`)
- `main`에 직접 커밋하지 않는다

### PR 생성 및 처리
- 내가 요구했을 때, [.github/TEMPLATE/PR.md](.github/TEMPLATE/PR.md) 템플릿에 맞춰 PR을 연다
- **Merge는 에이전트가 해도 된다** (2026-08-30 변경. 그 전에는 사용자 전용이었다).
  전제: `npm test`·`cargo test` 모두 통과 + 코드 리뷰 지적 반영 완료.
  force push·브랜치 삭제·이력 재작성은 여전히 사용자 확인을 받는다
- PR 하나는 하나의 마일스톤 항목([TODO.md](TODO.md) 체크박스) 단위를 넘지 않게 유지

### 문서 최신화
- 기능·범위가 바뀌면 [PRD.md](PRD.md)·[MOTIONS.md](MOTIONS.md)를 같은 PR에서 함께 수정한다
- 작업이 끝나면 [TODO.md](TODO.md)의 해당 체크박스를 체크한다
- 새로운 원칙이 생기면 [PRINCIPLE.md](PRINCIPLE.md)에 추가한다

### 시크릿 관리
- **앱은 네트워크를 쓰지 않고 토큰을 갖지 않는다** (PRD v3.0 §7). 보관할 시크릿이 없다
- 그래도 규칙은 남긴다 — API 토큰·OAuth 자격증명을 코드·문서·커밋에 넣지 않는다.
  토큰이 다시 필요해지는 기능은 PRINCIPLE 1에 걸리므로, 그런 기능이 나오면 먼저 멈추고 보고한다
