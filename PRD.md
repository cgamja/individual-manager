# 바탕화면 펭귄 — 쓸모없음을 목적으로 하는 macOS 상주 앱

> PRD v3.0 (2026-08-31) — 재미 전용으로 방향 전환. v2.0(2026-08-30) 초안은 업무 런처,
> v1.0(2026-08-06)은 업무 통합 앱이었다.
> 관련 문서: [PRINCIPLE.md](PRINCIPLE.md), [MOTIONS.md](MOTIONS.md)
>
> **v3.0에서 바뀐 것**: 업무 기능을 **전부** 뺐다. 뽀모도로 타이머, 서비스 런처,
> Claude Code 위임, Notion·Jira·Google Calendar 연동이 모두 범위 밖이다.
> 남는 것은 **바탕화면을 돌아다니는 펭귄 한 마리**뿐이고, 그것이 전부다.
> 왜 두 번이나 방향을 틀었는지는 [PRINCIPLE.md](PRINCIPLE.md) 개정 이력에 있다.

## 1. 개요

바탕화면에 펭귄 한 마리가 산다. 걷고, 헤엄치고, 자고, 낚시하고, 미끄러지고, 빽빽거리고,
가끔 발작한다. 때리면 방망이를 휘두르며 싸가지 없게 굴고, 집어 던지면 포물선을 그리며
날아가 벽에 튕긴다. 확장 모니터를 붙이면 그쪽까지 넘어가서 논다.

**이 앱은 아무 일도 하지 않는다.** 할 일을 알려주지 않고, 시간을 재지 않고, 브라우저를
열지 않는다. 일하다 고개를 들었을 때 펭귄이 뭔가 이상한 짓을 하고 있으면 성공이다.

**한 줄 정의**: 바탕화면에 사는 싸가지 없는 펭귄 한 마리.

## 2. 사용자 및 실행 환경

| 항목 | 확정 내용 |
|---|---|
| 사용자 | 본인 1명 (개인 도구, 배포 없음) |
| 실행 환경 | 개인 PC, macOS 전용, 로컬 실행 |
| 형태 | 메뉴바 상주 + **바탕화면 펭귄 한 마리**. 우클릭 → 설정 창 |
| 디스플레이 | **떠 있는 화면 하나.** 모니터 경계는 벽이다 (v3.2 개정, §5.2). 다른 화면으로는 드래그로만 옮긴다 |
| 네트워크 | 쓰지 않는다. 인증도, 토큰도, 외부 호출도 없다 |

## 3. 목표

1. **웃긴다.** 예측 가능한 마스코트가 아니라, 가끔 예상 밖의 짓을 하는 생물로 느껴진다.
2. **방해하지 않는다.** 포커스를 뺏지 않고, 기본값은 조용하며, 언제든 끌 수 있다.
3. **세계는 데스크톱 전체다.** 확장 모니터를 포함해 어디든 자유롭게 간다 (§5.2).
4. **앱은 하나, 펭귄 마릿수는 사용자가 고른다.** 앱이 두 개 뜨는 사고는 막고(§5.6),
   펭귄을 늘리고 줄이는 것은 우클릭으로 열어 둔다(§5.5). 기본은 한 마리.

## 4. 비목표 (Non-Goals)

- **업무 기능 일체** — 뽀모도로 타이머, 서비스 런처, Notion·Jira·Calendar 연동,
  Claude Code 위임. v1.0·v2.0의 모든 마일스톤이 여기 해당한다.
- **앱 인스턴스를 여러 개 띄우는 것** — 펭귄은 여러 마리여도 앱은 하나다 (§5.6)
- **육성·성장·재화·업적 같은 게임화** — 펭귄은 관리 대상이 아니라 룸메이트다.
  돌보지 않아도 알아서 산다.
- 다른 사용자 배포, 서버·클라우드, Windows/Linux 지원
- 업무 데이터든 무엇이든 로컬에 쌓는 것 (설정 제외, §7)

## 5. 기능 요구사항

### 5.1 펭귄 (M2.5에서 만든 것 — 유지한다)

- 바탕화면 위를 스스로 걸어다니고, 유휴 동작과 졸기를 오간다. 숨쉬기·눈 깜빡임은 항상
  돌고, 커서를 올리면 눈동자가 따라온다.
- **화면 전체를 무작위로 다닌다.** 바닥을 걷다가 가끔 공중으로 떠올라 날개를 저어
  헤엄쳐 간다. 오르내릴 때 진행 방향으로 몸을 눕힌다. **도착하면 대개 그대로
  떨어지고**(그래서 저절로 철푸덕·널브러짐이 나온다) 가끔 날개를 저어 내려앉는다 —
  비율과 그 대가는 [MOTIONS.md](MOTIONS.md)에 있다.
- 드래그로 집어 옮길 수 있다. **놓는 세기에 따라 포물선을 그리며 날아가고**, 벽과
  천장에서 감쇠하며 튕긴 뒤 착지한다. **착지는 세기에 따라 네 갈래다** — 살짝이면 그냥
  서고, 어중간하면 바닥에서 **통통 튀고**, 세게 박으면 **철푸덕**(배를 깔고 퍼진다),
  아주 세게면 **널브러진다**(바닥에 뻗어 한참 있다 일어난다).
  **던지기 속도의 상한은 세계 크기에 비례한다**
  (초당 세계를 **1.4번** 가로지르는 속도) — 고정 상한이면 좁은 화면에서 눈 깜짝할 새
  가로질러 버린다. 처음 넣은 0.9는 화면을 가로지르는 데 1.11초가 걸려 이번엔 던진
  것 같지가 않았다 (2026-09-02 조정).
- **왼쪽 클릭은 빠따다.** 펭귄이 야구방망이를 곧바로 한 번 후려친다. 1클릭 1회,
  연타 가능. **휘두른 마리는 제자리에 있다** — 맞은 펭귄이 스스로 날아가는 것은
  드래그로 던졌을 때와 **핀볼 모드(§5.8)**뿐이다(그때는 클릭이 채가 된다).
  다만 **휘두른 방망이가 보는 방향의 사거리 안에 있던 다른 마리는 날려 보낸다**
  (2026-09-02 추가). 세기는 던지기 상한보다 낮고, 날아간 마리는 착지 네 갈래를
  그대로 탄다. 자세한 규칙은 [MOTIONS.md](MOTIONS.md)에 있다.
- **싸가지 반응 5종** — 등 홱 돌리기, 고개만 돌려 외면, 날개 휘휘 젓기, 눈 굴리기,
  엉덩이 흔들기. 연달아 같은 반응이 나오지 않는다.
- **킹받는 한마디** — 클릭과 무관하게 7~18초 간격으로 알아서 말풍선을 띄운다.
  대사 목록은 사용자가 우클릭으로 연 창에서 추가·수정·삭제한다. 전부 지우면 조용해진다.
- 설정에서 끄고 켤 수 있고 선택은 유지된다. 기본은 켜짐.
- 키보드 포커스를 뺏지 않고 Cmd-Tab·Dock에 나타나지 않는다.
- 동작은 규칙 + 시드 난수로 만든다. **AI를 쓰지 않는다** (§8).

### 5.2 세계 = 펭귄이 떠 있는 화면 하나 (v3.2 개정 — 경계 넘기 폐기)

> **2026-08-31 (v3.2)** — 원래 이 절은 "세계 = 연결된 모든 화면"이었다.
> **모니터가 하나뿐이라 볼 일이 없어서** 경계 넘기를 범위 밖으로 뺐다. 근거와 되돌리는 비용은
> [PRINCIPLE.md](PRINCIPLE.md) 개정 이력에 있다 — **읽지 않으면 같은 논의를 처음부터
> 다시 하게 된다.** 폐기 전 본문(가상 사각형 vs 화면 목록 비교, 좌표 모델)은 git 이력에 있다.

- 펭귄은 `current_monitor()`가 준 **한 화면 안에서** 논다. 좌우 끝은 통로가 아니라 **벽**이고,
  걷다 닿으면 돌아서거나 굴러떨어진다 (MOTIONS "벽 반응은 두 갈래다").
- **다른 화면으로 옮기는 길은 드래그뿐이다.** 창은 이미 커서를 따라가고, 놓는 순간 그
  화면의 경계로 정산된다. 사용자의 손이지 펭귄의 자율 이동이 아니다.
- 던지기 속도 상한은 여전히 **세계의 가로 폭에 비례**한다 (MOTIONS). 세계가 화면 하나가
  되면서 그 폭이 곧 화면 폭이다.

**코어는 아직 화면 목록(`World`)을 받는다.** 브릿지가 화면 하나짜리 목록만 만들므로
프로덕션에서는 한 번도 여러 화면을 본 적이 없다 — **테스트만 붙들고 있는 죽은
일반화**이고, 걷어내는 것은 별도 작업이다 (`TODO.md`).

### 5.3 모션 (v3.0에서 늘린다)

동작 카탈로그의 단일 원천은 [MOTIONS.md](MOTIONS.md)다. v3.0에서 다섯을 새로 넣는다.

| 모션 | 무엇을 | 성격 |
|---|---|---|
| **얼음낚시** | 바닥에 앉아 구멍을 뚫고 낚싯대를 드리운다. 오래 기다리다 물고기를 잡거나 꽝 | 긴 유휴 |
| **슬라이딩** | 배를 깔고 미끄러진다. 걷기보다 빠르고 멀리 가며, 멈출 때 주르륵 밀린다 | 이동 |
| **빽빽거리기** | 몸을 부풀리고 날개를 퍼덕이며 빽빽댄다. 기본은 **무음**이고 소리는 opt-in (§5.5) | 반응 |
| **발작** | 드물게 터지는 광란 — 사방으로 튀며 몸을 떤다. 끝나면 아무 일 없었다는 듯 | 희귀 이벤트 |
| **굴러떨어지기** | 벽에 브레이크를 못 잡고 그대로 박아 뒤로 나자빠졌다가 일어난다 | 벽 반응 |

- 모션은 **화면 안 어디서 일어나는지**를 지킨다 — 얼음낚시와 슬라이딩은 바닥 동작이라
  공중에서는 시작하지 않는다.
- **벽 반응은 갈래를 갖는다.** 벽에 닿으면 늘 얌전히 돌아서던 것을 확률로 나눴다 —
  화면 끝이 완전히 예측 가능한 지점이면 매일 보는 장면 하나가 통째로 심심해진다.
  던져져서 벽에 부딪히는 것(`Thrown`)은 지금처럼 튕긴다. 이 갈래는 **혼자 걷다가**
  생기는 사건이다.
- 새 모션도 규칙과 시드 난수로만 고른다. 같은 모션이 연달아 나오지 않게 한다.
- 모션이 늘어날수록 "가끔 나오는 것"이 희석된다. 흔한 동작(걷기·유휴)과 희귀 동작
  (발작)의 빈도 차이를 분명히 둔다.
- **볼링(§5.9)과 비치발리볼(§5.10)은 이 표에 없다.** 저절로 나오는 동작이 아니라
  버튼으로만 시작하는 한 판이라 확률 사다리에 끼지 않는다 — 끼우면 뒤의 모든 확률이
  밀려 위 빈도 차이가 통째로 흐트러진다.

### 5.4 상호작용

| 조작 | 결과 |
|---|---|
| 왼쪽 클릭 | 빠따 한 번 + 싸가지 반응. **방망이 앞에 선 다른 마리는 날아간다**. **핀볼 모드에서는 채로 후려쳐 맞은 펭귄이 날아간다** (§5.8) |
| 드래그 | 집어 들기 → 놓는 세기대로 던져짐 (포물선·튕김) |
| 오른쪽 클릭 | 펭귄 옆에서 **설정 창**이 열린다 (§5.5) |
| 커서 올리기 | 눈동자가 따라온다 |
| 트레이 아이콘 | 메뉴바 밑에서 같은 설정 창이 열린다 |

### 5.5 설정 창 (팝오버에 남는 유일한 화면)

v2.0의 런처 카드와 타이머 카드를 걷어내고 **설정만** 남긴다.

- **펭귄 추가 / 이 펭귄 삭제** — 우클릭한 펭귄이 삭제 대상이다. 새 펭귄은 **부른 펭귄
  옆에** 나타나고 자기 시드를 갖고 따로 논다(같은 자리에서 같은 동작을 하면 한 마리가
  복제된 것처럼 보인다). 마릿수는 저장되어 다시 켜도 유지된다.
  **마지막 한 마리는 삭제할 수 없다** — 전부 없애는 것은 아래 on/off가 하는 일이고,
  두 장치가 같은 일을 다투면 안 된다. **상한은 8마리** — 창 하나가 웹뷰 하나라 메모리가
  붙는다. 트레이로 연 팝오버에는 삭제 대상이 없으므로 삭제 버튼이 비활성된다.
- 펭귄 on/off
- 대사 목록 편집 (추가·수정·삭제)
- **소리 on/off — 기본 꺼짐.** 켜면 **일곱에서만** 소리가 난다: 빠따 맞기(퍽)·날아가기(휙)·
  빽빽거리기(빽!)·발작(광란)·얼음낚시 잡았다(첨벙)·볼링 굴리기(드르륵)·볼링 핀 맞음(딱).
  기준은 "사용자가 방금 한 짓의 결과이거나,
  시간당 한 번보다 드물거나"다 — 잡았다는 이 규칙의 유일한 예외이고 근거는 `MOTIONS.md`
  효과음 절에 있다 — 착지·걷기·헤엄처럼 저절로 자주 나는 동작에 소리를 붙이면 상주 앱이
  시간당 150번 넘게 울린다 (`MOTIONS.md` 효과음 절). 상주 앱이 예고 없이 소리를 내면
  회의 중에 사고가 나므로 켜는 것은 사용자의 선택이다.
- **음량 — 다섯 단계, 가운데가 기본.** 단계마다 두 배(6dB)씩 커진다(기본의 ¼~4배).
  원래는 슬라이더를 두지 않기로 했었으나(시스템 볼륨이 하면 된다는 판단) 2026-09-01
  사용자 지시로 뒤집었다. 연속값이 아니라 단계인 이유: 결정적이라 테스트되고, 상한이
  헤드폰 사고를 막는 선으로 남는다.
- **테마 — 시스템 설정 / 라이트 / 다크** (2026-09-01 사용자 지시). 설정 창의 겉모습을
  정한다. **트레이 아이콘은 테마와 무관하게 항상 자동(템플릿 이미지)이다** — 메뉴바
  색은 OS만 정하므로 실루엣을 테마로 고정하면 시스템과 다를 때 메뉴바에 묻혀 안
  보이고, 트레이는 핀볼 모드의 "나가는 문 둘" 중 하나라 안 보이면 안 된다 (§5.8).
- **핀볼 on/off — 기본 꺼짐** (§5.8)
- **"볼링 한 판" 버튼** (§5.9). 토글이 아니라 버튼이다 — 볼링은 켜 두는 모드가 아니라
  몇 초짜리 한 판이고, 그래서 저장하지 않는다. 판이 도는 동안에는 비활성이다.
- **"비치발리볼 한 판" 버튼** (§5.10). 같은 이유로 버튼이고 저장하지 않는다.
  **두 판은 서로를 배제하므로 어느 쪽이든 도는 동안 버튼 둘이 함께 비활성된다.**
- 모션별 on/off는 두지 않는다 — 어떤 모션이 나올지 모르는 것이 재미다.
  **핀볼은 모션 토글이 아니라 세계의 규칙을 바꾸는 모드다** — 무엇이 나올지가 아니라
  부딪혔을 때 어떻게 되는지를 바꾼다.

### 5.6 앱 인스턴스는 하나

두 번째 실행은 **기존 인스턴스를 깨우고 스스로 물러난다.** 지금은 가드가 없어서 실수로
두 개가 뜨면 펭귄이 겹쳐 보이고, 설정을 고쳐도 다른 쪽은 모르고, 어느 쪽이 어떤 상태인지
알 수 없다. 개발 빌드와 설치된 `.app`이 같은 identifier(`com.kangr.penguin`)를 쓰므로
**둘을 동시에 띄우는 것도 막힌다** — 개발 중에는 설치된 앱을 종료하고 돌린다.

### 5.7 알림

**없다.** v2.0에 남아 있던 뽀모도로 종료 알림이 사라지면서 앱은 알림을 하나도 보내지
않는다. macOS 알림 권한도 요청하지 않고, `tauri-plugin-notification`도 걷어낸다.

### 5.8 핀볼 모드 (기본 꺼짐)

**펭귄이 공이 되고 커서가 채가 되는 모드다.** 설정에서 켜고 끈다.

- 착지 등급 판정(통통·철푸덕·널브러짐)을 **우회한다.** 감쇠가 거의 없어(0.92)
  벽·천장·바닥이 전부 반사면이 되고, 한 번 세게 떨어지면 스무 번 넘게 튄다.
- **왼쪽 클릭이 채다.** 맞은 지점의 반대 방향으로 펭귄이 날아간다 — 아래를 치면
  위로 뜬다. 커서가 펭귄 위에 있는 동안 방망이 모양으로 바뀌고, 그 범위가 곧 칠 수
  있는 범위다. 이 모드에서는 펭귄이 방망이를 휘두르지 않는다(§5.1의 예외).
- **앱 전역이다.** 여덟 마리를 켜 놨으면 여덟 마리가 전부 튄다.
- **다른 펭귄도 범퍼다.** 날아가던 펭귄이 다른 펭귄에 닿으면 **양쪽 다** 튕겨 나간다 —
  맞은 쪽만 날아가는 볼링과 다르다. 서 있던 펭귄도 얻어맞고 날아가므로 한 번의 타격이
  연쇄를 만든다. **평소 세계에서는 그대로 통과한다** — 걷는 펭귄들이 서로를 밀어내며
  붙어 다니면 그건 물리가 아니라 방해다.
- **지우는 게 아니라 가려두는 모드다.** 끄면 착지 네 갈래가 그대로 돌아온다.
- **켜 놓은 동안 화면 전체가 판이다.** **연결된 화면마다** 투명 창이 깔려 **커서가
  어디서나 방망이**가 되고, 바탕화면과 다른 앱으로 가는 클릭은 먹힌다. 방망이는 누르면 위로
  휘둘리고 떼면 내려온다. 판은 **펭귄 창보다 아래**에 깔리므로 펭귄을 클릭·드래그하는
  것은 지금까지와 같다.
- **나가는 문이 둘이다** — **Esc**와 **트레이 아이콘**. 화면 전체의 클릭을 먹는
  기능이라 되돌리는 길이 하나뿐이면 안 된다 (PRINCIPLE 5의 예외 항목). 판을 못 깔면
  모드가 스스로 되돌아온다.
- **점수·목숨·판 시작과 종료는 없다.** §4의 비목표(게임화)는 그대로 유효하다 —
  점수가 붙는 순간 "잘하려고" 보게 되는데, 이 앱은 보고 웃는 앱이다. 여덟 마리를
  한꺼번에 튕기는 편이 점수판보다 웃긴다.

### 5.9 볼링 (버튼으로 한 판)

**펭귄이 핀이 되고 사용자가 공을 굴린다.** 설정 창의 "볼링 한 판"으로 시작한다.

- **화면의 펭귄 전부가 참여한다.** 우클릭한 한 마리가 아니다 — 마릿수를 8까지 늘릴
  수 있는데 여덟 마리가 각자 따로 걷기만 하는 것이 이 항목이 생긴 이유다.
  **여러 마리가 하나의 사건에 함께 참여하는 첫 동작이다.**
- **화면 세로 중앙에서 한다. 바닥이 아니다.** 판이 뜨는 순간 펭귄들은 공중으로
  올라와 그 높이에 머문다 (2026-09-02 사용자 지시로 뒤집었다 — 처음에는 바닥이었다).
- **날아가서 선다.** 순간이동하지 않는다. 오른쪽에 **삼각형**으로 서고 **꼭짓점이
  공이 오는 왼쪽을 향한다.** 줄마다 1·2·3…으로 늘고 마지막 줄만 모자랄 수 있다.
  판을 바닥에서 화면 중앙으로 올린 것이 이걸 가능하게 했다 — 2차원 바닥은 선이라
  삼각형을 세울 수 없었다.
- **다 뜨면** 왼쪽에 공이 나타난다. 그 전에는 나타나지 않는다.
- **공은 마우스로 집어 굴린다.** 세기는 놓는 순간의 드래그가 정하고 세계 폭에
  비례한다 (던지기와 같은 관례). **수평으로만 굴러간다 — 조준 각도가 없다.**
  각도를 주는 순간 "잘 맞히려고" 보게 된다.
- **맞은 펭귄은 튕겨 나간다.** 제자리에서 도는 게 아니라 **평소 던져졌을 때와 같은
  물리**를 탄다 — 포물선을 그리고 벽에 튕기고 착지 네 갈래로 떨어진다. 그리고
  **튕겨 나간 펭귄이 옆 펭귄을 친다** (연쇄). 공은 속도를 조금 잃을 뿐 멈추지
  않는다 — 첫 펭귄에서 멈추면 마릿수가 무의미해진다.
- **소리가 난다** (효과음을 켰을 때, §5.5). 굴리기 시작할 때 드르륵, 핀이 맞을 때 딱.
- **공이 멎거나 화면을 벗어나면 판이 끝난다.** 펭귄들은 흩어져 평소로 돌아간다.
  살살 굴려 아무도 못 맞혀도 그냥 끝난다 — 재시도 버튼은 없고, 다시 하려면 버튼을
  다시 누른다.
- **점수·프레임·스트라이크·쓰러뜨린 마릿수를 세지도 보여주지도 않는다.**
- **화면을 덮지 않는다.** 판이 도는 동안에도 펭귄을 클릭·드래그할 수 있고 다른 앱도
  평소대로 쓸 수 있다. 판에서 펭귄을 끌어내면 그 마리는 판에서 빠진다 — 억지로
  자리로 되돌려 보내면 사용자와 앱이 싸운다.
- **저장하지 않는다** (§7). 켜 두는 모드가 아니라 몇 초짜리 한 판이라, 앱을 껐다
  켜면 판은 그냥 없다.

**왜 볼링에는 판의 끝이 있어도 되는가.** §5.8이 핀볼에서 "판 시작과 종료"를 거부한
근거는 *"점수가 붙는 순간 잘하려고 보게 된다"*였지 *"끝이 있으면 안 된다"*가 아니었다.
볼링의 끝은 성과 판정이 아니라 **공이 지나가면 상황이 끝난다는 물리적 사실**이다.
잘 굴렸든 헛굴렸든 똑같이 끝나고, 끝난 뒤에 남는 기록이 없다. §4의 게임화 비목표
(육성·성장·재화·업적)에는 넷 다 해당하지 않는다.

### 5.10 비치발리볼 (버튼으로 한 판, 관전형)

**펭귄들이 알아서 놀고 사용자는 구경만 한다.** 설정 창의 "비치발리볼 한 판"으로
시작한다. **§5.9와 정반대다** — 볼링은 사용자가 공을 굴리고, 이건 버튼을 누른 뒤
20초 동안 사용자가 **아무것도 하지 않는다.**

- **화면의 펭귄 전부가 참여한다.** 우클릭한 한 마리가 아니다 (§5.9와 같은 규칙).
- **짝수 두 마리부터다.** 한 마리면 팀이 안 나오므로 "두 마리부터 할 수 있어요"가 뜨고,
  **홀수면 판을 아예 열지 않는다** — "짝수 마릿수만 할 수 있어요"가 뜬다
  (2026-09-02 사용자 지시). 처음에는 한 팀이 한 마리 많게 두려 했는데, 그러면 그쪽이
  덜 뛴다. **이 판의 유일한 볼거리가 "누가 받으러 뛰는가"**라서(§5.10의 갈래 넷 중
  첫째) 그 차이가 한쪽으로 기울면 기능의 근거가 상한다. 거절하는 편이 정직하다.
- **화면 가운데로 모인다.** 날아가서 **화면 세로 중앙**에 뜬 채로 마주 서고,
  네트가 그 사이에 걸린다. **모래사장은 화면 바닥에 깔린다** (2026-09-02 사용자
  지시로 뒤집었다 — 처음에는 판도 바닥이었다).
  **판과 모래를 갈라 놓은 것이 이 뒤집기의 핵심이다.** 처음에는 "비치발리볼은
  모래 위에서 하는 운동이니 판도 바닥이라야 한다"고 봤는데, 그렇게 두면 판이
  화면 아래 한 줄에 눌려 **펭귄도 공도 네트도 전부 같은 띠 안에서 겹친다** —
  볼링이 판을 공중으로 올려 삼각형을 얻은 것과 정확히 같은 문제다. 판을 중앙으로
  올리면 공이 오르내릴 세로 공간이 생겨 **체공의 세 갈래(§아래 랠리)가 눈에
  보이고**, 해변은 배경으로 바닥에 남아 "비치"는 그대로 성립한다.
  **뛰는 그림은 안 잃었다** — 받으러 가는 것은 가로 이동이라 판의 높이와 무관하다.
- **훌라(luau) 차림을 한다.** 마른 풀색 라피아 치마에 목에는 레이(꽃목걸이).
  **수컷은 아래만, 암컷은 위아래 둘 다** — 전통 훌라 복식이 그렇다. 판이 도는
  동안에만이고, 그림도 성별도 **웹뷰가 갖는다** (§8): 성별은 창 라벨에서
  결정적으로 파생하므로 난수도 저장도 없고(§7), 앱을 껐다 켜도 같은 펭귄은 같은
  차림이다. **수영복이 아닌 이유는 적나라해서다** — 근거는 `MOTIONS.md`에 있다.
- **20초쯤 걸리고 알아서 끝난다.** 한 마리가 공을 띄워 서브하고, 넘어올 때마다 받는
  쪽에서 **한 마리가 뛰어나가** 받아 넘긴다. 어떤 공은 낮고 빠르게 꽂히고 어떤 공은
  하늘 높이 뜬다.
- **공이 모래에 닿으면 끝난다.** 마지막 공은 빈 곳에 꽂히고, 받으러 뛰던 마리가
  못 미친다. 판이 중앙이라 그 공은 **해변까지 길게 떨어진다** — 화면을 가로지르는
  낙하가 판의 끝을 알린다. **끝나면 선 그 자리에서** 평소 동작으로 돌아간다 —
  가로 자리를 원래 있던 곳으로 되돌려 보내지 않는다(공중이므로 그 자리에서 내려온다).
- **점수판이 없다.** 몇 대 몇인지 세지도, 보여주지도, 저장하지도 않는다. 다만
  **이긴 쪽은 엉덩이를 흔들고 진 쪽은 등을 홱 돌린다** — 싸가지 반응(§5.1)을 그대로
  쓴다. 기록이 아니라 반응이다.
- **사용자 입력이 없다.** 볼링의 드래그·굴리기에 해당하는 것이 하나도 없고, 랠리는
  전부 시드 난수로 만든다 (§8). 같은 시드는 같은 랠리를 낳는다.
- **화면을 덮지 않고 클릭을 먹지도 않는다.** 모래사장·네트·공은 그림뿐이라
  **클릭을 통과시킨다** — 판이 도는 동안에도 코트 위에서 다른 앱을 그대로 쓸 수 있고
  펭귄도 클릭·드래그할 수 있다. 판에서 펭귄을 끌어내면 그 마리는 판에서 빠진다.
  핀볼 판(§5.8)이 클릭을 **먹는** 근거는 "커서가 어디서나 채가 된다"였고, 여기엔
  그 근거가 없다.
- **볼링과 서로를 배제한다.** 어느 쪽이든 도는 동안 두 버튼이 함께 비활성된다 —
  둘 다 전 마리를 모는 판이라 동시에 열리면 한쪽이 상대 판의 마리를 끌어간다.
- **핀볼 모드는 켜 둔 채로도 된다.** 다만 판이 도는 20초 동안 **마리 간 충돌만
  쉰다** — 안 쉬면 코트 위에서 서로를 튕겨 랠리가 찢어진다. 판이 끝나면 돌아온다.
- **소리가 나지 않는다** (§5.5의 일곱에 들지 않는다). 근거는 `MOTIONS.md` 효과음 절에
  있다 — 버튼 한 번이 20초에 걸쳐 열몇 발을 낳으면 손짓과 소리의 연결이 다섯 번째
  타격쯤에서 끊기고, 남는 것은 배경 소음이다.
- **저장하지 않는다** (§7). 20초짜리 한 판이라 앱을 껐다 켜면 판은 그냥 없다.

**왜 비치발리볼에는 이긴 쪽과 진 쪽이 있어도 되는가.** §5.9가 볼링의 끝을 정당화한
근거(*"성과 판정이 아니라 공이 지나가면 상황이 끝난다는 물리적 사실"*)가 그대로
적용된다 — **공이 모래에 떨어지면 랠리가 끝난다는 것도 물리적 사실**이고, "1점 내기"는
기능이 아니라 **판의 길이**다. 그 위에 하나가 더 있다: 이기고 지는 것이 **기록으로
남지 않는다.** 누가 이겼는지는 900밀리초짜리 엉덩이 흔들기로만 존재하고, 그 뒤에
세지도 보여주지도 저장하지도 않는다. §4의 게임화 비목표(육성·성장·재화·업적)에는
넷 다 해당하지 않는다 — 반응은 기록이 아니다.

**왜 사용자가 아무것도 안 하는 기능을 넣는가.** 이 앱의 유일한 성공 기준은 "보고
있으면 웃긴가"다(§3). 지금까지의 모든 동작은 사용자가 보고 있지 않아도 돌아가거나
(걷기·낚시·발작), 사용자가 손을 대야 시작한다(빠따·던지기·볼링). **버튼 하나로
20초짜리 구경거리가 열리는 것**은 그 사이의 빈자리다. 그래서 이 기능의 위험도
다른 데 있다 — 물리가 틀리는 것이 아니라 **20초가 지루한 것**이고, 그 답(랠리를
매번 다르게 만드는 장치들)은 `MOTIONS.md`에 있다.

## 6. 제거되는 기능 (v2.0 → v3.0)

| 대상 | 상태 | 처리 |
|---|---|---|
| **뽀모도로 타이머** | main에 구현돼 있음 (`pomodoro.rs`, `timer_bridge.rs`, `TimerCard`, `lib/timer.ts`) | **코드 제거** |
| 타이머 종료 알림 | main에 구현돼 있음 (`lib/notification.ts`, 알림 플러그인) | **코드 제거** |
| **서비스 런처** (카드 4장 → Chrome) | `feat/m4-launcher-fan-01` 브랜치에만 있고 **main에 머지되지 않았다** | **머지하지 않고 버린다.** 브랜치는 이력으로 남긴다 |
| Notion TODO | v2.0(M3)에서 이미 제거 완료 | 없음 |
| Claude Code 위임 | 착수한 적 없음 | 폐기 |

제거 이유는 품질이 아니라 방향이다. 코드는 git 이력에 남으므로 되살릴 수 있다.

## 7. 앱이 저장하는 것

로컬 설정 파일(`tauri-plugin-store`)에 **설정만** 둔다.

1. 펭귄 on/off
2. 대사 목록
3. 소리 on/off
4. 음량 단계 (§5.5)
5. 테마 (§5.5)
6. 핀볼 on/off (§5.8)
7. 마릿수 (§5.5)

토큰·인증·업무 데이터는 없다. v1.0에서 확정했던 macOS Keychain 보관(Q2)은 v2.0에서
불필요해졌고, v3.0에서는 **보관할 대상 자체가 사라졌다.**

## 8. AI 사용 원칙

**앱은 AI를 쓰지 않는다.** v2.0에서 "AI가 연동의 핵심 엔진"이라고 뒤집었던 것은 연동이
있을 때의 이야기이고, 연동이 사라졌으므로 함께 사라진다.

- 펭귄의 동작·모션 선택·대사 추첨은 **규칙과 시드 난수**로 만든다. 충분하고, 오프라인에서
  돌고, 비용이 없고, 테스트할 수 있다.
- 대사를 AI로 생성하고 싶어지면 그때 다시 논의한다 — 지금 넣지 않는다.
- 이것은 앱의 런타임 이야기다. **개발에 Claude Code를 쓰는 것과는 무관하다.**

## 9. 오픈 퀘스천

| # | 항목 | 내용 | 상태 |
|---|---|---|---|
| Q9 | 효과음 소스 | **직접 합성으로 확정** (Web Audio API) — 음원 파일을 쓰지 않아 라이선스·출처 표기가 없고 번들이 늘지 않는다. 마리마다 목소리(음높이)를 값으로 다르게 만들 수 있는 것도 합성이라 가능하다 | 확정 ✅ |
| ~~Q7~~ | ~~다중 화면 좌표계~~ | **v3.2에서 무효** — 경계 넘기를 빼면서 물어볼 일이 없어졌다 (§5.2). 확정했던 "화면별 `Bounds` 목록"은 코드에 남아 있지만 화면 하나만 담는다 | 폐기 ✅ |
| Q8 | **발작 트리거** | **순수 저확률 무작위로 확정** (§5.3) — 연타 트리거는 빽빽거리기가 가져갔고, 같은 입력에 두 반응을 걸면 서로 희석된다. 무엇보다 발작은 "아무 일 없었다는 듯 돌아온다"가 정의라 **원인이 없어야 성립한다** — 원인이 있으면 그건 화지 발작이 아니다. 30,000번에 한 번(≈ 며칠에 한 번) | 확정 ✅ |
| Q6 | 소리 기본값 | **기본 꺼짐으로 확정** (§5.5) — 상주 앱이 예고 없이 소리를 내면 사고가 난다 | 확정 ✅ |
| Q1 | 기술 스택 | **Tauri v2 + React/TypeScript(Vite)** — M1·M2·M2.5에서 검증 완료 | 확정 ✅ |
| Q2~Q5 | 토큰 보관 / Claude Code 연동 / 런처 UX / Chrome 창 제어 | v3.0에서 **전부 무효** — 대상 기능이 사라졌다 | 폐기 ✅ |

## 10. 마일스톤

v1.0·v2.0의 M1~M6은 닫힌다. v3.0은 `F`(fun) 번호로 새로 센다.

| 단계 | 내용 | 상태 |
|---|---|---|
| M1~M2.5 | 메뉴바 골격 · Notion TODO · 바탕화면 펭귄 | 완료 (M2는 제거됨) |
| M3~M6 | Notion 제거 · 런처 · 타이머 연동 · Claude Code 위임 | **v3.0에서 폐기** |
| **F1** | **걷어내기** — 뽀모도로·알림 제거, 팝오버를 설정만 남긴 상태로 (§6) | |
| ~~**F2**~~ | ~~**세계 넓히기**~~ — **v3.2에서 폐기** (§5.2). 좌표계 교체(U1)만 머지된 채 남았다 | 폐기 ✅ |
| **F3** | **모션 늘리기** — 얼음낚시·슬라이딩·빽빽거리기·발작 (§5.3) | |

**F1 → F3 순서인 이유**: 타이머를 안고 가면 모든 리팩터링이 두 배가 된다.

**F2가 사라진 뒤**로는 "모션을 만들기 전에 좌표계를 먼저 바꾼다"는 제약도 같이 사라졌다.
그 제약이 F2를 F3 앞에 두게 했던 유일한 이유였다.
