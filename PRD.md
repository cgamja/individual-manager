# 업무에만 집중하기 위한 개인 총괄 비서 Desktop App

> PRD 초안 v1.0 (2026-08-06) — PM 인터뷰 결과 기반. 관련 문서: [PRINCIPLE.md](PRINCIPLE.md), [SERVICES.md](SERVICES.md)

## 1. 개요

업무 중 Jira, Notion, Google Calendar, Slack, Webex를 오가며 확인·기록하는 데 드는 시간과 집중력 손실을 없애기 위한 **개인용 macOS 상주 데스크톱 앱**. 메뉴바의 펭귄 아이콘을 누르면 카드 기반 팝오버 UI가 열리고, 그 안에서 5개 협업 툴의 조회와 조작(양방향)을 모두 처리한다. 뽀모도로 타이머를 내장한다.

**한 줄 정의**: 브라우저를 열지 않고, 펭귄 하나로 하루의 업무 흐름(할 일 확인 → 집중 → 기록 → 소통)을 끝내는 개인 비서.

## 2. 사용자 및 실행 환경

| 항목 | 확정 내용 |
|---|---|
| 사용자 | 본인 1명 (개인 도구, 배포 없음) |
| 실행 환경 | 개인 PC, macOS 전용, 로컬 실행 |
| 형태 | 메뉴바 상주 앱. 펭귄 아이콘(`penguin-icon.png`) 클릭 → 카드형 팝오버 UI |
| 인증 | 개인 API 토큰 / 개인 OAuth (앱 심사·멀티테넌트 불필요) |

## 3. 목표

1. 5개 툴(Jira, Notion, Google Calendar, Slack, Webex)을 **한 UI에서 양방향으로** 조회·조작한다.
2. 웹사이트(브라우저)를 열 필요를 없앤다 — 원래 툴 접속은 실패 케이스로 간주한다.
3. TODO·일정의 기록은 **Notion Database에 그대로 남긴다** (앱은 클라이언트, Notion이 유일한 저장소).
4. 원할 때 물어보는 pull 방식이 기본이고, 내가 고른 이벤트만 macOS 알림으로 push 받는다.
5. 동기화는 자동이고 신뢰 가능해야 한다 (주기적 검증 + 오류 시 전체 재동기화 — PRINCIPLE.md).

## 4. 비목표 (Non-Goals)

- 다른 사용자 배포, 멀티 계정, 서버/클라우드 인프라
- AI 자동 요약·자동 일정 추출 (v1은 전부 수동/규칙 기반. §8 참고)
- 태스크 연결형 뽀모도로, 시간 기록 역전파(Jira 워크로그 등) — 뽀모도로는 독립 타이머
- Slack DM·스레드 전체 기능 (읽기 + 특정 채널 전송까지만)
- 앱 자체 로컬 DB에 업무 데이터 저장 (설정·캐시 제외)
- Windows/Linux 지원

## 5. 기능 요구사항

### 5.1 뽀모도로 타이머 (독립형)
- 기본 25분 집중 / 5분 휴식 타이머. 시간 커스터마이즈 가능.
- 태스크와 연결하지 않는다. 세션 기록·리포트 없음 (v1).
- 종료 시 macOS 알림 (§5.7).
- 메뉴바 또는 팝오버에서 남은 시간 확인 가능.

### 5.2 TODO / 일정 — Notion (Source of Truth)
- 기존 Notion Database(캘린더/TODO)를 읽고 쓰는 클라이언트.
- **저장 원칙**: 모든 내용은 Notion에 저장. 앱 삭제 시에도 데이터는 Notion에 온전히 남는다.
- **데이터 모델 (2026-08-09 실물 확인 후 확정, R3)**: 기존 DB는 **행 하나가 "하루"** 이고, 할 일은
  그 날짜 페이지 **본문의 체크박스 블록**으로 들어간다. 앱은 이 구조를 바꾸지 않고 그대로 쓴다.
  - 행 속성: `이름`(Title), `날짜`(Date), `수행도`(Select) — 상세는 [SERVICES.md](SERVICES.md)
  - 할 일 = 날짜 페이지 본문의 `to_do` 블록
  - 카테고리 = 본문 헤딩 (`공부` / `기타`) — DB 속성이 아니다
  - 시간 = 할 일 텍스트의 접두어 규칙 (예: `09:30 ~ 17:30 AWS 교육`)
- 기존 템플릿의 한계(운동·공부·기타가 섞여 있고 시간·세부사항 기록 불가)는 **DB 속성 추가가 아니라
  블록 표현 규칙과 앱 UI로 해결**한다. 이미 손으로 쓰던 시간 표기를 규칙으로 정식화하고, 앱이
  입력·파싱을 담당한다 (AI 없이 키워드/패턴 규칙 — §8).
- 완료 처리 상태: **완료 / 일부 / 미완 / 기타** 4단계 — 기존 `수행도` Select를 그대로 쓰며,
  **하루 단위 달성도**를 뜻한다 (할 일 하나의 상태가 아니다). `기타`는 휴일·휴가처럼 할 일이 없는 날.
- 앱에서 할 일 생성·수정·체크·조회, 그리고 하루의 수행도 변경 모두 가능.

### 5.3 Jira
- 나에게 할당된 티켓 목록·상세 내용 조회.
- 앱에서 직접: 완료(상태 전환) 처리, 댓글 작성.
- 스프린트 진행상황 확인.

### 5.4 Google Calendar
- 일정 등록 / 수정 / 삭제 / 조회.
- **수동 브릿지**: Slack 메시지, Notion TODO, Jira 티켓, Webex 공지를 보다가 "일정으로 등록" 액션으로 캘린더에 추가한다. 판단은 항상 사용자가 한다 (자동 추출 없음).

### 5.5 Slack
- 내가 선택한 채널의 메시지 조회.
- 특정 채널로 메시지 전송.

### 5.6 Webex
- 공지가 올라오는 스페이스의 메시지 모아보기.
- **키워드 필터/하이라이트** (예: "배포", "장애", 내 이름)로 필요한 내용을 캐치.

### 5.7 알림 (opt-in)
- **기본은 pull**: 앱은 조용하고, 내가 열어서 물어볼 때 보여준다.
- 원하는 항목만 골라 알림을 켤 수 있다. 지원 이벤트 (전부 v1 범위):
  1. 뽀모도로 타이머 종료
  2. 다가오는 캘린더 일정 (N분 전, 설정 가능)
  3. 지켜보는 Slack 채널의 새 메시지
  4. 지켜보는 Webex 스페이스의 새 공지 (키워드 매칭 포함)
  5. Jira 티켓 변화 (나에게 할당 / 내 티켓에 댓글)

## 6. 동기화 및 신뢰성 (PRINCIPLE.md 반영)

- 연동은 자동이어야 하며, 한 번의 동기화 실패도 기록 유실로 이어지면 안 된다.
- **주기적 검증**: 폴링 주기마다 로컬 캐시와 원격 상태를 비교 검증한다.
- **오류 시 전체 재동기화**: 검증 실패 시 해당 서비스 전체 동기화로 복구한다.
- 쓰기 실패(예: Notion API 오류) 시 재시도 큐에 넣고, 실패가 지속되면 사용자에게 명시적으로 알린다.

## 7. 통합별 인증 — 확인된 사실

| 서비스 | 확인 결과 | 인증 방식 |
|---|---|---|
| Jira | Cloud, API 토큰 발급 성공 ✅ | 개인 API Token |
| Notion | Integration 생성 가능 ✅ | Internal Integration Token |
| Google Calendar | 개인 Google 계정 ✅ | 개인 GCP 프로젝트 + OAuth (테스트 앱) |
| Slack | 앱 생성 + "Install to Workspace" 버튼 확인 ✅ | 개인 Slack App (User/Bot Token) |
| Webex | 개발자 포털 토큰 발급 확인 ✅ | ⚠️ 개인 액세스 토큰은 12시간 만료 → **OAuth Integration 방식 필수** |

## 8. AI 사용 원칙

- **v1은 AI를 사용하지 않는다** (PRINCIPLE.md). 요약·캐치·일정 판단은 전부 사용자 수동 + 키워드 규칙으로 해결한다.
- 어쩔 수 없이 AI가 필요해지는 기능이 생기면: **Claude 구독 OAuth(개인 인증)** 를 사용해 별도 API 비용 없이 붙인다. 이 경우에도 opt-in 기능으로 격리한다.

## 9. 리스크 & 오픈 퀘스천

| # | 항목 | 내용 | 상태 |
|---|---|---|---|
| R1 | Webex 토큰 수명 | 개인 토큰 12h 만료. OAuth Integration + refresh token 흐름 구현 필요 | 설계 반영 |
| R2 | Slack 설치 승인 | "Install" 버튼은 확인했으나 실제 설치·스코프 승인까지는 미검증 | 개발 초기에 검증 |
| R3 | Notion DB 스키마 | **확인 완료** — 상태 속성 `수행도`(완료/미완/일부/기타)가 이미 존재해 그대로 사용한다. 다만 **행 하나가 "하루"** 이고 할 일은 본문 체크박스 블록이라, 속성 확장이 아닌 **블록 기반**으로 M2를 설계한다 (§5.2). 기존 80행 마이그레이션 불필요 | 해소 ✅ |
| R4 | API Rate Limit | 5개 서비스 폴링 병행 시 호출량. 서비스별 폴링 주기 차등 설계 필요 | 설계 시 결정 |
| R5 | 범위 크기 | 양방향 통합 5종은 개인 프로젝트로 큰 범위. 마일스톤 순서로 리스크 관리 (§10) | 인지됨 |
| Q1 | 기술 스택 | **Tauri v2 + React/TypeScript(Vite)** 확정 — 상주 메모리 부담과 TS 경험 활용 기준으로 선택. M1에서 검증 완료 | 확정 ✅ |
| Q2 | 토큰 보관 | **macOS Keychain** 확정 — Rust `keyring` crate로 저장·조회한다. 토큰은 Rust 프로세스 밖으로 나가지 않고 외부 API 호출도 Rust가 수행하며, 웹뷰에는 연결 여부만 전달한다 | 확정 ✅ |

## 10. 마일스톤 제안

전부 v1 범위지만, 개발은 아래 순서로 진행해 각 단계마다 "혼자 쓰기에 유용한 상태"를 유지한다.

| 단계 | 내용 | 이유 |
|---|---|---|
| M1 | 메뉴바 앱 골격 + 펭귄 팝오버 UI + 뽀모도로 타이머 + 알림 ① | 외부 의존성 없이 앱 뼈대와 UX 검증 |
| M2 | Notion TODO 통합 (스키마 확장 포함) + 상태 4단계 | Source of truth부터. 매일 쓰는 핵심 루프 |
| M3 | Jira 통합 (티켓 조회·완료·댓글·스프린트) + 알림 ⑤ | 가장 구체적인 불편(사이트 접속) 해소 |
| M4 | Google Calendar 통합 + "일정으로 등록" 브릿지 + 알림 ② | 흩어진 일정의 집결지 완성 |
| M5 | Slack 통합 (채널 읽기·전송) + 알림 ③ | 메시지 계열 시작 |
| M6 | Webex 통합 (OAuth, 공지 모아보기, 키워드 캐치) + 알림 ④ | 인증이 가장 까다로워 마지막 |
