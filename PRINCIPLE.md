# 서비스가 지켜야 할 원칙

> 관련 문서: [PRD.md](PRD.md), [SERVICES.md](SERVICES.md)

### 1. AI를 사용하지 않는다
AI OAuth나 API는 별도 비용이 발생한다. 이 서비스는 개인적으로 쓰는 서비스이기 때문에, 오로지 내 CPU 및 코드로 동작하도록 한다.
요약·캐치·일정 판단 같은 기능은 사용자 수동 조작 + 키워드 규칙으로 해결한다.

**예외**: 어쩔 수 없이 AI가 필요한 기능이 생기면, 별도 API 비용이 들지 않는 **Claude 구독 OAuth(개인 인증)** 방식으로만 붙인다. 이 경우에도 해당 기능은 opt-in으로 격리해, AI 없이도 서비스 전체가 동작해야 한다.

### 2. 별도 웹사이트를 열고 작업하지 않도록 한다
이 서비스는 메뉴바(또는 바탕화면)에 있는 **'펭귄'을 눌러서** 모든 작업을 하는 용도다.
알림은 허용되지만, Chrome 등 브라우저를 열어 원래 툴 사이트에서 작업하게 만들면 이 서비스의 UX는 실패한 것이다.
연동된 서비스의 조회·조작(양방향)은 모두 앱 안에서 끝나야 한다.

### 3. SERVICES가 자동으로 연동되어야 한다
연동이 되지 않으면 서비스 이용에 큰 차질이 생긴다.
한 번이라도 동기화가 실패하면 기록에 차질이 생기므로, 아래 방법을 기본으로 하고 필요하면 추가 방법을 고안해 사용한다.

- a) 항상 또는 주기적으로 검증한다 (로컬 캐시 ↔ 원격 상태 비교)
- b) 검증 중 오류가 발견되면 해당 서비스를 전체 동기화하여 복구한다
- c) 쓰기 실패는 재시도 큐로 처리하고, 실패가 지속되면 사용자에게 명시적으로 알린다

### 4. 데이터는 원본 서비스에 남긴다
이 앱은 클라이언트일 뿐, 업무 데이터의 저장소가 아니다.
TODO·일정은 Notion Database에, 일정은 Google Calendar에 저장되어 앱을 지워도 데이터가 온전해야 한다.
앱 로컬에는 설정과 캐시만 둔다.