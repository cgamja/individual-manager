# 서비스가 지켜야 할 원칙

> 관련 문서: [PRD.md](PRD.md), [SERVICES.md](SERVICES.md)
>
> **개정 (2026-08-30)**: 원칙 1·2·3을 방향 전환에 맞춰 바꿨다. 개정 전 원칙은 문서 맨 아래
> "개정 이력"에 남긴다 — 왜 뒤집혔는지 모르면 다음에 같은 논의를 처음부터 다시 한다.

### 1. Claude Code가 연동의 실행 엔진이다

Notion·Jira·Google Calendar와의 조회·기록은 앱이 각 서비스의 API 클라이언트를 직접 구현하지
않고 **Claude Code에 위임한다.** 이미 구독 중이라 별도 API 비용이 들지 않는다는 것이 이
선택의 근거다(개정 전 원칙 1이 지키려던 것도 결국 비용이었다).

- 모델은 작업 성격에 따라 섞어 쓴다 — 판단이 필요하면 Opus, 일반 작업은 Sonnet,
  단순 조회·분류는 Haiku.
- 앱은 각 서비스의 스키마·API 버전·페이지네이션을 알 필요가 없다. 그건 Claude Code의 몫이다.

**주의**: `--dangerously-skip-permissions`로 돌린다는 것은 **Claude Code가 확인 없이 파일을
쓰고 명령을 실행한다**는 뜻이다. 개인 기기에서 본인만 쓰는 도구라는 전제 위에서만 성립하며,
이 전제가 깨지면(공유 기기, 원격 실행, 신뢰할 수 없는 입력을 그대로 전달) 다시 검토해야 한다.

### 2. 앱은 런처이고, 깊은 작업은 원래 서비스에서 한다

**개정 전 원칙("브라우저를 열게 만들면 실패")을 뒤집는다.** 5개 서비스의 양방향 UI를 앱 안에
다시 만드는 것은 개인 프로젝트가 감당할 범위를 넘고, 만들어도 원래 웹 UI보다 나을 수 없다는
것이 M2(Notion TODO)를 실제로 만들어 보고 얻은 결론이다.

- 펭귄을 누르면 **데스크톱 앱 아이콘처럼 서비스 목록이 뜨고**, 고르면 그 서비스의 웹 UI가
  Chrome 창으로 열린다.
- 앱이 직접 소유하는 화면은 **펭귄과 뽀모도로 타이머**뿐이다.
- 조회·기록을 앱 안에서 끝내고 싶을 때는 UI를 만드는 대신 Claude Code에게 말한다(원칙 1).

### 3. 앱은 업무 데이터를 캐시하지 않는다

앱이 각 서비스의 클라이언트를 그만두면서 "로컬 캐시 ↔ 원격 검증 / 전체 재동기화 / 쓰기 재시도
큐"라는 개정 전 원칙 3의 장치도 함께 불필요해졌다. 동기화할 로컬 사본이 없으면 어긋날 것도 없다.

- 앱 로컬에는 **설정만** 둔다 (펭귄 on/off, 타이머 시간, 런처에 띄울 서비스 목록).
- 상태가 필요하면 그때 Claude Code나 웹 UI에서 직접 읽는다.

### 4. 데이터는 원본 서비스에 남긴다

이 앱은 클라이언트일 뿐, 업무 데이터의 저장소가 아니다.
TODO·일정은 Notion Database에, 일정은 Google Calendar에 저장되어 앱을 지워도 데이터가 온전해야 한다.
**개정에서 유일하게 그대로인 원칙이고, 오히려 더 강해졌다** — 앱이 캐시조차 두지 않기 때문이다.

---

## 개정 이력

### 2026-08-30 — 앱 내장 통합에서 런처 + Claude Code 위임으로

**개정 전 원칙 1**: "AI를 사용하지 않는다." 요약·캐치·일정 판단을 사용자 수동 조작 +
키워드 규칙으로 해결하고, AI가 필요하면 Claude 구독 OAuth로만 opt-in 격리한다.
→ 근거였던 "별도 비용"은 Claude Code 구독으로 해소된다. 다만 "AI 없이도 서비스 전체가
동작해야 한다"는 단서는 더 이상 성립하지 않는다 — 이제 연동의 핵심이 AI다.

**개정 전 원칙 2**: "별도 웹사이트를 열고 작업하지 않도록 한다." 브라우저를 열게 만들면
UX는 실패한 것이고, 연동 서비스의 조회·조작은 모두 앱 안에서 끝나야 한다.
→ 정면으로 뒤집혔다. M2에서 Notion TODO 하나의 양방향 UI를 만드는 데 든 비용(플랜 5개,
PR 6개)에 비해 얻은 것이 원래 Notion 웹 UI보다 낫지 않았다. 나머지 4개 서비스에 같은
비용을 다시 치르는 대신, 앱은 진입점만 제공하고 작업은 원래 서비스에서 한다.

**개정 전 원칙 3**: "SERVICES가 자동으로 연동되어야 한다" — 주기 검증, 오류 시 전체
재동기화, 쓰기 재시도 큐.
→ 앱이 캐시를 들지 않으므로 장치 자체가 불필요해졌다. 원칙 3은 "캐시하지 않는다"로 대체됐다.
